{
  "name": "dumshare",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "db:push": "drizzle-kit push --force"
  },
  "devDependencies": {
    "drizzle-kit": "^0.31.5",
    "drizzle-orm": "^0.45.2",
    "better-sqlite3": "^12.1.1",
    "vitest": "^3.2.4"
  }
}
