---
phase: 02-ledger-setup-and-participants
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/domain/events/types.ts
  - src/domain/projections/types.ts
  - src/domain/projections/replay.ts
  - src/tests/ledger-setup-participants.spec.ts
autonomous: true
requirements:
  - LEDR-01
must_haves:
  truths:
    - "Organizer can create a ledger identity with title and settlement context while offline."
    - "Ledger creation events replay into the same ledger metadata every time."
  artifacts:
    - path: "src/domain/events/types.ts"
      provides: "Typed ledger creation and participant event contracts"
      exports: ["EventInput", "LedgerEvent"]
    - path: "src/domain/projections/types.ts"
      provides: "Ledger projection fields for title and settlement context"
      exports: ["LedgerProjection"]
    - path: "src/domain/projections/replay.ts"
      provides: "Deterministic replay support for ledger.created"
      exports: ["replayLedger"]
    - path: "src/tests/ledger-setup-participants.spec.ts"
      provides: "Automated phase tests for ledger setup replay behavior"
  key_links:
    - from: "src/domain/events/types.ts"
      to: "src/domain/projections/replay.ts"
      via: "eventType switch dispatch"
      pattern: "case \"ledger.created\""
    - from: "src/tests/ledger-setup-participants.spec.ts"
      to: "src/domain/projections/replay.ts"
      via: "replay assertion"
      pattern: "replayLedger\("
---

<objective>
Lock the ledger-creation domain contracts and deterministic replay behavior required for offline organizer setup.

Purpose: Establish the canonical in-app representation of a trip ledger (title + settlement context) so later participant and expense features build on a stable base.
Output: Typed event/projection contracts plus failing-then-passing tests for ledger creation replay.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/domain/events/types.ts
@src/domain/projections/types.ts
@src/domain/projections/replay.ts
@src/tests/local-data-backbone.spec.ts

<interfaces>
From src/domain/events/repository.ts:
```typescript
export type EventRepository = {
  appendEvent(input: AppendEventInput): Promise<void>;
  listEventsByLedger(ledgerId: string): Promise<StoredEvent[]>;
};
```

From src/domain/projections/replay.ts:
```typescript
export function replayLedger(events: LedgerEvent[]): LedgerProjection;
```

From src/domain/projections/types.ts:
```typescript
export type LedgerProjection = {
  ledgerId: string;
  lastSequence: number;
  appliedEventIds: string[];
  entries: LedgerEntry[];
};
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define typed ledger-setup contracts for replay</name>
  <files>src/domain/events/types.ts, src/domain/projections/types.ts</files>
  <behavior>
    - Test 1: Type-level shape supports eventType "ledger.created" with title + settlement context payload.
    - Test 2: Projection contract exposes ledger metadata fields consumed by UI/API in later phases.
  </behavior>
  <action>Update event and projection type contracts to represent organizer ledger setup per LEDR-01, including ledger title and settlement context fields in projection output. Preserve existing append-only event contracts from Phase 1 and do not introduce mutable update/delete event semantics.</action>
  <verify>
    <automated>npm run test -- local-data-backbone</automated>
  </verify>
  <done>Type contracts compile and existing local-data-backbone tests still pass with no regression.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement deterministic replay coverage for ledger.created</name>
  <files>src/domain/projections/replay.ts, src/tests/ledger-setup-participants.spec.ts</files>
  <behavior>
    - Test 1: replayLedger applies ledger.created and yields projection containing title + settlement context.
    - Test 2: replayLedger returns identical projection for the same event sequence across repeated runs.
    - Test 3: Unknown event types still throw explicit errors (Phase 1 guardrail retained).
  </behavior>
  <action>Create a dedicated phase test file and implement replay support for "ledger.created" per LEDR-01. Keep deterministic sequence ordering and strict unsupported-event rejection behavior from Phase 1. Do not add UI placeholders or deferred sync functionality in this phase.</action>
  <verify>
    <automated>npm run test -- ledger-setup-participants</automated>
  </verify>
  <done>Ledger creation replay tests pass, deterministic replay assertions pass, and unsupported-event regression remains enforced.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| command/test input → event payload | Untrusted values cross into persisted append-only event rows |
| event log rows → replay projector | Persisted JSON is parsed into user-visible ledger metadata |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | T | `src/domain/projections/replay.ts` payload parser | mitigate | Validate ledger-created payload structure before projection mutation; throw explicit errors on invalid fields. |
| T-02-02 | R | `src/domain/projections/replay.ts` event dispatch | mitigate | Keep strict default switch branch (`Unsupported eventType`) to prevent silent acceptance of tampered event types. |
| T-02-03 | D | `src/domain/projections/replay.ts` ordering | mitigate | Preserve sequence-based ordering before replay so out-of-order inputs cannot produce unstable projections. |
</threat_model>

<verification>
Run:
- `npm run test -- ledger-setup-participants`
- `npm run test -- local-data-backbone`
</verification>

<success_criteria>
- `ledger.created` events deterministically produce ledger metadata projection with title and settlement context.
- Phase-specific ledger setup tests pass in CI-style non-interactive execution.
- Existing Phase 1 invariants remain green.
</success_criteria>

<output>
After completion, create `.planning/phases/02-ledger-setup-and-participants/02-ledger-setup-and-participants-01-SUMMARY.md`
</output>
