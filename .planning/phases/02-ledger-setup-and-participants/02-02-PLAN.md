---
phase: 02-ledger-setup-and-participants
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - src/domain/events/types.ts
  - src/domain/projections/types.ts
  - src/domain/projections/replay.ts
  - src/tests/ledger-setup-participants.spec.ts
autonomous: true
requirements:
  - LEDR-02
must_haves:
  truths:
    - "Organizer can add participant names to an existing ledger fully offline."
    - "Participant names remain in reconstructed ledger state after replay/reopen simulation."
    - "Participant roster keeps deterministic order using event sequence."
  artifacts:
    - path: "src/domain/events/types.ts"
      provides: "Typed participant-added event payload contract"
      exports: ["EventInput", "LedgerEvent"]
    - path: "src/domain/projections/types.ts"
      provides: "Projection participant roster shape"
      exports: ["LedgerProjection"]
    - path: "src/domain/projections/replay.ts"
      provides: "Replay support for participant.added"
      exports: ["replayLedger"]
    - path: "src/tests/ledger-setup-participants.spec.ts"
      provides: "Automated participant persistence/determinism tests"
  key_links:
    - from: "src/domain/projections/replay.ts"
      to: "src/domain/projections/types.ts"
      via: "participant roster append"
      pattern: "participants"
    - from: "src/tests/ledger-setup-participants.spec.ts"
      to: "src/domain/events/repository.ts"
      via: "appendEvent/listEventsByLedger for reopen simulation"
      pattern: "appendEvent\(|listEventsByLedger\("
---

<objective>
Implement participant roster replay behavior so organizer-added names are durable and deterministic in offline ledger state.

Purpose: Satisfy participant management requirement needed before contributor onboarding and expense splitting phases.
Output: Participant event/projection support with automated persistence and deterministic replay tests.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-ledger-setup-and-participants/02-01-PLAN.md
@src/domain/events/repository.ts
@src/domain/events/types.ts
@src/domain/projections/types.ts
@src/domain/projections/replay.ts

<interfaces>
From src/domain/events/repository.ts:
```typescript
export function createEventRepository(db: LedgerDb): EventRepository;
```

From src/domain/projections/types.ts (after 02-01):
```typescript
export type LedgerProjection = {
  ledgerId: string;
  lastSequence: number;
  appliedEventIds: string[];
  title: string;
  settlementContext: string;
  entries: LedgerEntry[];
};
```

From src/domain/projections/replay.ts:
```typescript
export function replayLedger(events: LedgerEvent[]): LedgerProjection;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend contracts for participant roster events</name>
  <files>src/domain/events/types.ts, src/domain/projections/types.ts</files>
  <behavior>
    - Test 1: participant.added payload requires participantId and displayName.
    - Test 2: projection shape exposes participants list needed for later split assignment flows.
  </behavior>
  <action>Extend contracts to include organizer-driven participant roster changes per LEDR-02. Participant data must remain name-based passive roster entries in this phase (no contributor identity/device binding yet; that belongs to Phase 3).</action>
  <verify>
    <automated>npm run test -- ledger-setup-participants</automated>
  </verify>
  <done>Contracts compile with participant roster fields and tests cover payload/projection expectations.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement participant.added replay and offline persistence tests</name>
  <files>src/domain/projections/replay.ts, src/tests/ledger-setup-participants.spec.ts</files>
  <behavior>
    - Test 1: replayLedger includes added participant names in reconstructed projection.
    - Test 2: reopening DB and replaying persisted events yields same participant roster.
    - Test 3: roster ordering follows event sequence deterministically.
  </behavior>
  <action>Add replay support for `participant.added` events per LEDR-02 and extend phase test suite to append participant events through repository, then assert deterministic roster reconstruction before and after reopen. Keep append-only repository API unchanged and preserve strict unknown-event failure behavior.</action>
  <verify>
    <automated>npm run test -- ledger-setup-participants</automated>
  </verify>
  <done>Participant roster reconstruction is deterministic, persists across reopen simulation, and tests are green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| organizer input → participant event payload | User-entered names cross into persisted immutable event log |
| persisted events → roster projection | Stored event payloads are transformed into user-visible participant roster |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-04 | T | `src/domain/projections/replay.ts` participant payload parser | mitigate | Validate participant payload fields (`participantId`, `displayName`) before adding to projection. |
| T-02-05 | I | `src/domain/projections/types.ts` participant model | mitigate | Keep participant projection limited to passive name roster data in Phase 2; exclude device/auth fields to avoid premature exposure. |
| T-02-06 | D | `src/tests/ledger-setup-participants.spec.ts` replay determinism guardrails | mitigate | Add deterministic ordering + reopen replay tests so non-deterministic roster behavior is caught automatically. |
</threat_model>

<verification>
Run:
- `npm run test -- ledger-setup-participants`
- `npm test`
</verification>

<success_criteria>
- Organizer-origin participant names are reconstructable from append-only events without network calls.
- Participant roster stays stable across identical replay and reopen simulations.
- Scope remains Phase-2-only (passive participants; no contributor onboarding flows).
</success_criteria>

<output>
After completion, create `.planning/phases/02-ledger-setup-and-participants/02-ledger-setup-and-participants-02-SUMMARY.md`
</output>
