---
phase: 04-expense-capture-foundations
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/domain/events/types.ts
  - src/domain/projections/types.ts
  - src/domain/projections/replay.ts
  - src/tests/expense-capture-foundations.spec.ts
autonomous: true
requirements:
  - EXPS-01
  - EXPS-02
  - EXPS-03
must_haves:
  truths:
    - "Organizer can create an offline expense with required fields (description, currency, total amount, date) per EXPS-01."
    - "Claimed contributor can create an offline expense with required fields per EXPS-02."
    - "Expense creation requires one-or-more explicit payer rows with participantId and paid amount per EXPS-03."
    - "Invalid payload, unknown payer participant, or unauthorized creator fails replay with deterministic plain-language errors (D-07, D-08)."
  artifacts:
    - path: "src/domain/events/types.ts"
      provides: "Strict expense.created payload contract including creator context and payer breakdown"
      exports: ["KnownEventType", "EventInput", "LedgerEvent"]
    - path: "src/domain/projections/types.ts"
      provides: "LedgerEntry fields for total/date/creator and payer breakdown"
      exports: ["LedgerEntry", "LedgerProjection"]
    - path: "src/domain/projections/replay.ts"
      provides: "Deterministic validation and application of expense.created with creator + payer guards"
      exports: ["replayLedger"]
    - path: "src/tests/expense-capture-foundations.spec.ts"
      provides: "Automated contract and replay invariant coverage for EXPS-01/02/03"
  key_links:
    - from: "src/domain/events/types.ts"
      to: "src/domain/projections/replay.ts"
      via: "expense.created parser and replay switch branch"
      pattern: "parseExpenseCreatedPayload|case \"expense.created\""
    - from: "src/domain/projections/replay.ts"
      to: "src/domain/projections/types.ts"
      via: "LedgerEntry append with payer rows and creator metadata"
      pattern: "projection\.entries\.push"
    - from: "src/tests/expense-capture-foundations.spec.ts"
      to: "src/domain/projections/replay.ts"
      via: "replay assertions for success and guard errors"
      pattern: "replayLedger\("
---

<objective>
Implement deterministic offline expense-capture foundations for organizer and contributors with strict multi-payer validation.

Purpose: Satisfy EXPS-01/02/03 while honoring locked Context decisions D-01..D-11 and preserving event-sourced strict replay guarantees.
Output: Updated expense contracts, replay logic, and automated phase tests proving authorized creation + payer integrity.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-expense-capture-foundations/04-CONTEXT.md
@.planning/phases/04-expense-capture-foundations/04-RESEARCH.md
@.planning/phases/04-expense-capture-foundations/04-PATTERNS.md
@src/domain/events/types.ts
@src/domain/projections/types.ts
@src/domain/projections/replay.ts
@src/domain/onboarding/authority.ts

<interfaces>
From src/domain/events/types.ts:
```typescript
export type KnownEventType =
  | "ledger.created"
  | "participant.added"
  | "invite.issued"
  | "invite.revoked"
  | "invite.consumed"
  | "expense.created"
  | "expense.note-added";
```

From src/domain/projections/types.ts:
```typescript
export type LedgerEntry = {
  expenseId: string;
  description: string;
  amountMinor: number;
  currency: string;
  createdAt: string;
  createdByDeviceId: string;
  sourceEventId: string;
};

export type LedgerProjection = {
  participants: LedgerParticipant[];
  participantContributorDeviceClaims: Record<string, string>;
  syncHubDeviceId: string;
};
```

From src/domain/projections/replay.ts:
```typescript
export function replayLedger(events: LedgerEvent[]): LedgerProjection;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend expense contracts and write failing capture invariants</name>
  <files>src/domain/events/types.ts, src/domain/projections/types.ts, src/tests/expense-capture-foundations.spec.ts</files>
  <read_first>src/domain/events/types.ts, src/domain/projections/types.ts, src/tests/ledger-setup-participants.spec.ts, src/tests/contributor-onboarding-invitations.spec.ts, .planning/phases/04-expense-capture-foundations/04-CONTEXT.md</read_first>
  <behavior>
    - Test 1: Contract surface contains strict required `expense.created` payload fields for description/currency/total/date/creator/payers (D-01, D-02, EXPS-01/02/03).
    - Test 2: Projection contract includes payer rows and creator metadata required for deterministic replay (D-03, D-04).
    - Test 3: Spec has failing expectations before implementation for new payload and projection fields.
  </behavior>
  <action>Per D-01/D-02/D-03/D-04, update `src/domain/events/types.ts` with explicit `ExpenseCreatedPayload` including `expenseId`, `description`, `currency`, `totalAmountMinor`, `expenseDate`, `creatorRole` (`"organizer" | "contributor"`), and `payers: { participantId: string; paidAmountMinor: number }[]`. Update `src/domain/projections/types.ts` `LedgerEntry` to include `totalAmountMinor`, `expenseDate`, `creatorRole`, and payer breakdown. Create `src/tests/expense-capture-foundations.spec.ts` in existing style with contract assertions and replay invariant placeholders that initially fail until replay logic is implemented.</action>
  <acceptance_criteria>
    - `src/domain/events/types.ts` contains `type ExpenseCreatedPayload` with literal fields `totalAmountMinor`, `expenseDate`, `creatorRole`, and `payers`.
    - `src/domain/projections/types.ts` `LedgerEntry` contains `totalAmountMinor`, `expenseDate`, `creatorRole`, and payer list shape.
    - `src/tests/expense-capture-foundations.spec.ts` contains tests referencing `EXPS-01`, `EXPS-02`, and `EXPS-03` behaviors and `expense.created`.
  </acceptance_criteria>
  <verify>
    <automated>npm run test -- expense-capture-foundations</automated>
  </verify>
  <done>Expense contract and projection contract are explicit, strict, and test-addressable for organizer/contributor creation with payer breakdown.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement replay authorization and payer validation for expense.created</name>
  <files>src/domain/projections/replay.ts, src/tests/expense-capture-foundations.spec.ts</files>
  <read_first>src/domain/projections/replay.ts, src/domain/projections/types.ts, src/domain/events/types.ts, src/domain/onboarding/authority.ts, src/tests/expense-capture-foundations.spec.ts, .planning/phases/04-expense-capture-foundations/04-CONTEXT.md</read_first>
  <behavior>
    - Test 1: Organizer actor can create expense with required fields and one-or-more payer rows (EXPS-01, EXPS-03).
    - Test 2: Claimed contributor actor can create expense with required fields and payer rows (EXPS-02, D-05, D-09).
    - Test 3: Unknown payer participant ID throws deterministic plain-language error (D-06, D-07).
    - Test 4: Unauthorized creator device throws deterministic plain-language error and event is not applied (D-05, D-07, D-08).
    - Test 5: Invalid payload shape throws `Invalid payload for eventType expense.created` (D-02, D-08).
  </behavior>
  <action>In `src/domain/projections/replay.ts`, replace the legacy `parseExpenseCreatedPayload` shape with strict validation for all fields from Task 1, including non-empty strings, integer amounts, and payer array length >= 1. Validate each payer row has non-empty `participantId` and positive integer `paidAmountMinor`. Enforce D-06 by ensuring each payer participant exists in `projection.participants`. Enforce D-05/D-09 by allowing creation only when actor is organizer (`projection.syncHubDeviceId`) or a claimed contributor device (`Object.values(projection.participantContributorDeviceClaims)`). On violation, throw plain-language deterministic errors (e.g., `Only organizer or a claimed contributor device can create expenses`, `Expense payer references unknown participant`). Append validated entry into `projection.entries` with new fields; preserve sequence ordering and strict default unsupported-event throw (D-08).</action>
  <acceptance_criteria>
    - `src/domain/projections/replay.ts` contains updated `parseExpenseCreatedPayload` validating `totalAmountMinor`, `expenseDate`, `creatorRole`, and `payers`.
    - Replay switch branch for `expense.created` includes organizer-or-claimed-contributor guard and payer participant existence guard.
    - `src/tests/expense-capture-foundations.spec.ts` asserts success for organizer + contributor and `toThrow` checks for unauthorized actor and unknown payer participant.
    - Regression suites for prior phases remain green.
  </acceptance_criteria>
  <verify>
    <automated>npm run test -- expense-capture-foundations && npm run test -- contributor-authority-policy && npm run test -- ledger-setup-participants && npm test</automated>
  </verify>
  <done>Phase 4 expense creation behavior is deterministic, authorization-safe, and fully covered by automated tests without regressing prior phase invariants.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| event payload JSON → replay parser | Untrusted payload data crosses into immutable event history processing |
| actorDeviceId → expense authorization guard | Potential spoofed creator identity attempts privileged write behavior |
| payer participant references → projection participant roster | Foreign or stale participant IDs can corrupt expense integrity |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | T | `src/domain/projections/replay.ts` expense payload parser | mitigate | Validate every required expense field and payer row type/shape; throw deterministic invalid-payload errors before mutation. |
| T-04-02 | E | `src/domain/projections/replay.ts` creator authorization branch | mitigate | Permit `expense.created` only when actor is organizer sync hub or claimed contributor device; reject all others with explicit guard error. |
| T-04-03 | I | `src/domain/projections/replay.ts` payer participant validation | mitigate | Enforce payer participant IDs exist in projection participant roster; reject unknown IDs to prevent integrity drift in downstream balances. |
| T-04-04 | R | `src/tests/expense-capture-foundations.spec.ts` | mitigate | Add automated invariant tests for each guard and error path so bypasses are caught in CI. |
</threat_model>

<verification>
- `npm run test -- expense-capture-foundations`
- `npm run test -- contributor-authority-policy`
- `npm run test -- ledger-setup-participants`
- `npm test`
</verification>

<success_criteria>
- EXPS-01: Organizer expense creation contract and replay behavior exist with required fields and passing tests.
- EXPS-02: Claimed contributor expense creation contract and replay behavior exist with required fields and passing tests.
- EXPS-03: Multi-payer breakdown with explicit paid amounts is required and replay-validated for one-or-more payers.
- Invalid payloads and unauthorized or unknown references fail deterministically with plain-language errors.
</success_criteria>

<output>
After completion, create `.planning/phases/04-expense-capture-foundations/04-expense-capture-foundations-01-SUMMARY.md`
</output>
