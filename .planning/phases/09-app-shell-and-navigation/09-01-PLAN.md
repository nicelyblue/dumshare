---
phase: 09-app-shell-and-navigation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - App.tsx
  - src/navigation/routes.ts
  - src/navigation/types.ts
autonomous: true
requirements:
  - FE-01
must_haves:
  truths:
    - "User can open the app into a native mobile shell with a branded header and primary navigation."]
    - "The app has typed route names and screen contracts for the five main frontend areas."
    - "Expo-compatible navigation dependencies are declared so the shell can mount a real navigator in Phase 9."
  artifacts:
    - path: "package.json"
      provides: "Expo-compatible navigation dependencies needed by the shell"
      exports: ["dependencies", "devDependencies"]
    - path: "App.tsx"
      provides: "Root Expo entry point that renders the branded shell"
      exports: ["default"]
    - path: "src/navigation/routes.ts"
      provides: "Named route registry for dashboard, setup, expense entry, sync, and balances"
      exports: ["APP_ROUTES", "APP_ROUTE_NAMES"]
    - path: "src/navigation/types.ts"
      provides: "Typed navigation param list and screen contract definitions"
      exports: ["AppRouteName", "RootStackParamList"]
  key_links:
    - from: "App.tsx"
      to: "src/navigation/routes.ts"
      via: "primary destination buttons / landing shell state"
      pattern: "APP_ROUTES|APP_ROUTE_NAMES"
    - from: "src/navigation/types.ts"
      to: "src/navigation/routes.ts"
      via: "typed route names constrain registry entries"
      pattern: "RootStackParamList|AppRouteName"
---

<objective>
Establish the Expo app shell foundation and typed route contract for the frontend milestone.

Purpose: Deliver FE-01 by turning the current placeholder root into a branded mobile shell and adding the route metadata that Phase 9's navigation layer will consume.
Output: Updated Expo dependencies, root shell entry, and typed route contract files.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/domain/projections/types.ts
@src/domain/balances/types.ts

<interfaces>
From `App.tsx`:
```typescript
export default function App(): JSX.Element;
```

From `package.json`:
```json
{
  "dependencies": {
    "expo": "^55.0.19",
    "expo-dev-client": "^55.0.30"
  }
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Expo navigation dependencies and route contracts</name>
  <files>package.json, src/navigation/routes.ts, src/navigation/types.ts</files>
  <action>Add the Expo-compatible navigation stack needed for Phase 9. Use the Expo-native React Navigation package set rather than custom routing so later screens can share one shell: `@react-navigation/native`, `@react-navigation/native-stack`, `react-native-screens`, `react-native-safe-area-context`, and `react-native-gesture-handler`. Create `src/navigation/routes.ts` as the single source of truth for the five main frontend destinations: dashboard, setup, expense entry, sync, and balances. Create `src/navigation/types.ts` with a `RootStackParamList` and route-name union so the shell and future screens cannot drift from the registry.</action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>The app has typed route metadata and Expo-compatible navigation dependencies declared for the shell.</done>
</task>

<task type="auto">
  <name>Task 2: Replace the placeholder root with a branded shell entry</name>
  <files>App.tsx, src/navigation/routes.ts, src/navigation/types.ts</files>
  <action>Replace the static title/subtitle view in `App.tsx` with a branded Expo shell entry that shows the Dumshare header, a short product line, and the primary destination labels from the route registry. Keep this root entry intentionally lightweight in Phase 9: it should prove the app opens into a native shell and expose the main areas as primary actions, while the full multi-screen navigation container is completed in Plan 02.</action>
  <verify>
    <automated>npx tsc --noEmit && npm test</automated>
  </verify>
  <done>The root entry is no longer a placeholder and presents the phase's primary navigation destinations in a branded shell.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| route registry -> shell entry | Screen names and labels must stay in sync so the shell cannot link to nonexistent destinations |
| user tap -> screen navigation | UI interaction should only transition between known in-app destinations |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01 | T | `src/navigation/routes.ts` | mitigate | Keep a single typed route registry and reuse it in App.tsx so labels and destinations cannot diverge. |
| T-09-02 | D | `App.tsx` shell entry | mitigate | Keep the root shell lightweight and deterministic so layout state does not depend on hidden async data. |
| T-09-03 | R | `src/navigation/types.ts` | mitigate | Type navigation params explicitly to make route transitions and downstream screen contracts auditable. |
</threat_model>

<verification>
- `npx tsc --noEmit`
- `npm test`
</verification>

<success_criteria>
- FE-01 is satisfied: the app opens into a branded mobile shell instead of a plain placeholder.
- Route names for the five main frontend areas are typed and centralized.
- The root entry is ready for the full navigation container built in Plan 02.
</success_criteria>

<output>
After completion, create `.planning/phases/09-app-shell-and-navigation/09-01-SUMMARY.md`
</output>