---
phase: 09-app-shell-and-navigation
plan: 02
type: execute
wave: 2
depends_on:
  - 09-01
files_modified:
  - App.tsx
  - src/navigation/AppNavigator.tsx
  - src/navigation/featureRegistry.ts
  - src/ui/AppShell.tsx
  - src/ui/FeatureCard.tsx
  - src/screens/FeatureScreen.tsx
autonomous: true
requirements:
  - FE-02
must_haves:
  truths:
    - "User can move between dashboard, setup, expense entry, sync, and balances screens without leaving the app."
    - "Each main area renders inside the same branded app shell and shares the same frame and header treatment."
    - "The navigation structure is driven by a registry rather than hardcoded screen-condition branches."
  artifacts:
    - path: "src/navigation/AppNavigator.tsx"
      provides: "Primary navigation container for the frontend milestone"
      exports: ["AppNavigator"]
    - path: "src/navigation/featureRegistry.ts"
      provides: "Destination labels, order, and placeholder content for the five main areas"
      exports: ["FEATURE_REGISTRY"]
    - path: "src/ui/AppShell.tsx"
      provides: "Shared branded shell frame and layout spacing"
      exports: ["AppShell"]
    - path: "src/ui/FeatureCard.tsx"
      provides: "Reusable destination card used by the main navigation surface"
      exports: ["FeatureCard"]
    - path: "src/screens/FeatureScreen.tsx"
      provides: "Generic placeholder screen used by the five route destinations"
      exports: ["FeatureScreen"]
  key_links:
    - from: "src/navigation/AppNavigator.tsx"
      to: "src/navigation/featureRegistry.ts"
      via: "registry-backed screen list"
      pattern: "FEATURE_REGISTRY"
    - from: "src/navigation/AppNavigator.tsx"
      to: "src/ui/AppShell.tsx"
      via: "shell wrapping the navigation container"
      pattern: "AppShell"
    - from: "src/screens/FeatureScreen.tsx"
      to: "src/ui/FeatureCard.tsx"
      via: "screen actions rendered as reusable cards"
      pattern: "FeatureCard"
---

<objective>
Build the actual navigation container and shared shell primitives for the frontend milestone.

Purpose: Deliver FE-02 by letting the user move between all primary app areas from one consistent mobile shell.
Output: Navigation container, feature registry, reusable shell primitives, and generic feature screens.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-app-shell-and-navigation/09-01-PLAN.md
@src/navigation/routes.ts
@src/navigation/types.ts

<interfaces>
From `src/navigation/routes.ts`:
```typescript
export const APP_ROUTES = {
  dashboard: "dashboard",
  setup: "setup",
  expenseEntry: "expense-entry",
  sync: "sync",
  balances: "balances",
} as const;
```

From `src/navigation/types.ts`:
```typescript
export type RootStackParamList = {
  dashboard: undefined;
  setup: undefined;
  expenseEntry: undefined;
  sync: undefined;
  balances: undefined;
};
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the shared shell and destination card primitives</name>
  <files>src/ui/AppShell.tsx, src/ui/FeatureCard.tsx</files>
  <action>Create a shared `AppShell` component that gives all main screens the same branded frame, spacing, and safe-area behavior. Add a reusable `FeatureCard` component that turns route metadata into a consistent touch target for the navigation surface. Keep the styling intentional and travel-friendly: strong contrast, clear hierarchy, and no generic boilerplate layout.</action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>Every frontend destination can reuse the same branded shell and card styling instead of re-implementing chrome per screen.</done>
</task>

<task type="auto">
  <name>Task 2: Wire the registry-driven app navigator and placeholder feature screens</name>
  <files>App.tsx, src/navigation/AppNavigator.tsx, src/navigation/featureRegistry.ts, src/screens/FeatureScreen.tsx</files>
  <action>Build the real navigation container that renders all five main areas from the route registry and mounts each area inside the shared shell. Use the route registry to define order, labels, and placeholder copy for the dashboard, setup, expense entry, sync, and balances screens. Render the root app through this navigator so the app can move between destinations without leaving the shell. Keep each feature screen generic in Phase 9; later milestone phases will replace the placeholder content with the actual product flows.</action>
  <verify>
    <automated>npx tsc --noEmit && npm test</automated>
  </verify>
  <done>The app can navigate between the five primary frontend areas from one shell, and the structure is ready for later screen-specific milestone work.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| registry metadata -> rendered screen | Screen labels and placeholder content must stay aligned with route order |
| navigator -> shared shell | The shell should not be bypassed when moving between the app's primary areas |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-04 | T | `src/navigation/featureRegistry.ts` | mitigate | Centralize screen order and labels in one registry consumed by the navigator and cards. |
| T-09-05 | I | `src/ui/AppShell.tsx` | mitigate | Keep shell chrome consistent across screens so users always know they are inside the same app. |
| T-09-06 | D | `src/navigation/AppNavigator.tsx` | mitigate | Use a bounded, registry-driven route set rather than dynamic screen creation to avoid unstable navigation state. |
</threat_model>

<verification>
- `npx tsc --noEmit`
- `npm test`
</verification>

<success_criteria>
- FE-02 is satisfied: the user can move between dashboard, setup, expense entry, sync, and balances screens inside one app.
- Shared shell and card primitives are reusable by later frontend phases.
- The navigation structure is driven by a registry, not hardcoded branches.
</success_criteria>

<output>
After completion, create `.planning/phases/09-app-shell-and-navigation/09-02-SUMMARY.md`
</output>