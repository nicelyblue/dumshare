---
phase: 01-local-data-backbone
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - src/domain/projections/replay.ts
  - src/domain/projections/types.ts
  - src/domain/projections/index.ts
  - src/tests/local-data-backbone.spec.ts
autonomous: true
requirements:
  - DATA-03
  - DATA-01
must_haves:
  truths:
    - "Replaying the same event history always produces the same ledger view."
    - "Persisted events reconstruct user-visible state after app restart."
  artifacts:
    - path: "src/domain/projections/replay.ts"
      provides: "Deterministic event replay reducer"
    - path: "src/domain/projections/types.ts"
      provides: "Typed ledger projection contract"
    - path: "src/tests/local-data-backbone.spec.ts"
      provides: "Replay determinism regression tests"
  key_links:
    - from: "src/domain/projections/replay.ts"
      to: "src/domain/events/repository.ts"
      via: "event list input to replay reducer"
      pattern: "listEventsByLedger"
    - from: "src/tests/local-data-backbone.spec.ts"
      to: "src/domain/projections/replay.ts"
      via: "same-input replay equality assertions"
      pattern: "toEqual"
---

<objective>
Implement deterministic replay projection so ledger outcomes are reproducible from append-only events.

Purpose: Deliver DATA-03 and complete the phase outcome of stable user-visible ledger reconstruction.
Output: Deterministic replay projector and regression tests proving replay invariance.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-local-data-backbone/01-01-PLAN.md
@.planning/phases/01-local-data-backbone/01-RESEARCH.md

<interfaces>
From `src/domain/events/types.ts` (created in 01-01):

```typescript
export type LedgerEvent = {
  id: string;
  ledgerId: string;
  eventType: string;
  eventVersion: number;
  occurredAt: string;
  actorDeviceId: string;
  sequence: number;
  payloadJson: string;
};
```

From `src/domain/events/repository.ts` (created in 01-01):

```typescript
export function appendEvent(event: LedgerEventInput): Promise<void>;
export function listEventsByLedger(ledgerId: string): Promise<LedgerEvent[]>;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend tests with failing deterministic replay cases</name>
  <files>src/tests/local-data-backbone.spec.ts</files>
  <read_first>src/tests/local-data-backbone.spec.ts, src/domain/events/types.ts, .planning/ROADMAP.md</read_first>
  <behavior>
    - Test 1: identical ordered event input arrays replay to identical projection output snapshots (DATA-03).
    - Test 2: events loaded from persistence after simulated reopen replay to the same projection as pre-close replay (DATA-01 + DATA-03).
    - Test 3: unsupported eventType is rejected with explicit error (tampering hardening).
  </behavior>
  <action>Add new failing tests in `src/tests/local-data-backbone.spec.ts` that call `replayLedger(events)` twice with the same fixture and assert deep equality; include reopen path by reading events through `listEventsByLedger` after reconnect; add explicit negative case for unknown event type to force strict replay contract.</action>
  <acceptance_criteria>
    - Test file contains `replayLedger` test blocks for deterministic equality and reopen reconstruction.
    - `npm run test -- local-data-backbone` fails before replay implementation is added.
  </acceptance_criteria>
  <verify>
    <automated>npm run test -- local-data-backbone</automated>
  </verify>
  <done>Replay determinism expectations are encoded as failing automated tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement deterministic replay projection and typed ledger view</name>
  <files>src/domain/projections/types.ts, src/domain/projections/replay.ts, src/domain/projections/index.ts, src/tests/local-data-backbone.spec.ts</files>
  <read_first>src/domain/events/types.ts, src/domain/events/repository.ts, src/tests/local-data-backbone.spec.ts, .planning/research/ARCHITECTURE.md</read_first>
  <behavior>
    - Replay is deterministic for stable ordered input.
    - Replay output includes enough user-visible state to validate phase goal (event count + per-ledger latest sequence + reconstructed entries).
  </behavior>
  <action>Create `LedgerProjection` type with concrete fields `ledgerId`, `lastSequence`, `appliedEventIds`, and `entries`; implement `replayLedger(events: LedgerEvent[]): LedgerProjection` in `src/domain/projections/replay.ts` using stable `sequence` ordering and exhaustive `switch(eventType)` dispatch; export projector through `src/domain/projections/index.ts`; satisfy tests including unknown event rejection and reopen replay invariance.</action>
  <acceptance_criteria>
    - `src/domain/projections/replay.ts` exports `replayLedger`.
    - `replayLedger` sorts/processes events by `sequence` deterministically.
    - Unknown event type path throws explicit error message containing `Unsupported eventType`.
    - `npm run test -- local-data-backbone` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>npm run test -- local-data-backbone</automated>
  </verify>
  <done>Deterministic replay projector is implemented and regression-tested.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| persisted events → projection reducer | Stored data is treated as untrusted input until validated during replay dispatch. |
| projector output → user-visible ledger views | Incorrect deterministic logic can silently corrupt displayed balances/history. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-05 | T | `src/domain/projections/replay.ts` | mitigate | Validate eventType via exhaustive switch and throw on unsupported values to block tampered event payloads. |
| T-01-06 | R | `src/tests/local-data-backbone.spec.ts` | mitigate | Add replay-equality and reopen reconstruction tests to guarantee reproducible audit traces. |
| T-01-07 | D | `src/domain/projections/replay.ts` | accept | Replay performance under very large logs is deferred to later hardening phases; current phase scope ensures correctness first. |
| T-01-08 | I | `src/domain/projections/types.ts` | mitigate | Restrict projection output to required ledger fields and avoid leaking raw sensitive payload internals into UI models. |
</threat_model>

<verification>
- `npm run test -- local-data-backbone` passes.
- `npm test` passes.
</verification>

<success_criteria>
- DATA-03: replaying identical event history always yields identical projection output.
- Phase 1 goal complete: local append-only events reconstruct stable user-visible state.
</success_criteria>

<output>
After completion, create `.planning/phases/01-local-data-backbone/01-local-data-backbone-02-SUMMARY.md`
</output>
