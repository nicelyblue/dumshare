---
phase: 01-local-data-backbone
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - drizzle.config.ts
  - src/data/sqlite/client.ts
  - src/data/sqlite/schema.ts
  - src/domain/events/types.ts
  - src/domain/events/repository.ts
  - src/tests/local-data-backbone.spec.ts
autonomous: true
requirements:
  - DATA-01
  - DATA-02
must_haves:
  truths:
    - "Ledger data persists on-device and reloads without internet access."
    - "Ledger write history is append-only and immutable."
  artifacts:
    - path: "src/data/sqlite/client.ts"
      provides: "Local SQLite bootstrap and connection lifecycle"
    - path: "src/data/sqlite/schema.ts"
      provides: "Typed event-log and metadata tables"
    - path: "src/domain/events/repository.ts"
      provides: "Append/read-only event storage API"
    - path: "src/tests/local-data-backbone.spec.ts"
      provides: "Persistence and append-only behavior tests"
  key_links:
    - from: "src/domain/events/repository.ts"
      to: "src/data/sqlite/client.ts"
      via: "SQLite client dependency injection"
      pattern: "appendEvent\("
    - from: "src/tests/local-data-backbone.spec.ts"
      to: "src/domain/events/repository.ts"
      via: "Automated append-only assertions"
      pattern: "expect\(.*append"
---

<objective>
Establish the local persistence foundation and append-only event storage contracts for the ledger.

Purpose: Satisfy DATA-01 and DATA-02 before deterministic replay is layered in later plans.
Output: SQLite/Drizzle storage bootstrap, append-only event repository, and executable tests.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-local-data-backbone/01-RESEARCH.md
@.planning/phases/01-local-data-backbone/01-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create failing tests for local persistence and append-only invariants</name>
  <files>src/tests/local-data-backbone.spec.ts</files>
  <read_first>package.json, .planning/REQUIREMENTS.md, .planning/phases/01-local-data-backbone/01-RESEARCH.md</read_first>
  <behavior>
    - Test 1: appending an event stores it locally and it is readable after reopening the database connection (DATA-01).
    - Test 2: repository API exposes append/read operations only and rejects update/delete mutation attempts for event rows (DATA-02).
    - Test 3: event row ordering is stable by deterministic append sequence key.
  </behavior>
  <action>Create `src/tests/local-data-backbone.spec.ts` with explicit fixtures for `ledgerId`, `eventId`, `eventType`, `eventVersion`, `occurredAt`, `payloadJson`, and `deviceId`; write failing tests first for local reopen persistence and append-only behavior, then keep those tests as phase guardrails.</action>
  <acceptance_criteria>
    - `src/tests/local-data-backbone.spec.ts` contains test names including "persists events across reopen" and "does not mutate prior events".
    - Running `npm run test -- local-data-backbone` fails before implementation is added.
  </acceptance_criteria>
  <verify>
    <automated>npm run test -- local-data-backbone</automated>
  </verify>
  <done>Failing automated tests exist and encode DATA-01/DATA-02 behaviors unambiguously.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement SQLite + Drizzle event-store contracts to satisfy tests</name>
  <files>package.json, drizzle.config.ts, src/data/sqlite/client.ts, src/data/sqlite/schema.ts, src/domain/events/types.ts, src/domain/events/repository.ts</files>
  <read_first>src/tests/local-data-backbone.spec.ts, .planning/research/STACK.md, .planning/research/ARCHITECTURE.md</read_first>
  <behavior>
    - Test suite passes with local-only SQLite persistence.
    - Event repository allows append/read paths while preventing mutation paths.
  </behavior>
  <action>Implement Drizzle SQLite schema and repository with concrete fields: `events(id TEXT PRIMARY KEY, ledger_id TEXT, event_type TEXT, event_version INTEGER, occurred_at TEXT, actor_device_id TEXT, payload_json TEXT, sequence INTEGER)` and `sync_checkpoints(peer_id TEXT PRIMARY KEY, last_sequence INTEGER)`; add `openLedgerDb(dbName: string)` in `src/data/sqlite/client.ts`; implement `appendEvent(input)` and `listEventsByLedger(ledgerId)` in repository and do not implement any update/delete method for events; wire package scripts so `npm run test -- local-data-backbone` executes the new suite.</action>
  <acceptance_criteria>
    - `src/domain/events/repository.ts` exports `appendEvent` and `listEventsByLedger`.
    - `src/domain/events/repository.ts` does not export `updateEvent` or `deleteEvent`.
    - `src/data/sqlite/schema.ts` contains `events` table with the exact column names listed in action.
    - `npm run test -- local-data-backbone` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>npm run test -- local-data-backbone</automated>
  </verify>
  <done>Local event storage and append-only persistence contracts are implemented and tests pass.</done>
</task>

<task type="auto">
  <name>Task 3: [BLOCKING] Push local schema before plan verification</name>
  <files>drizzle.config.ts, src/data/sqlite/schema.ts</files>
  <read_first>drizzle.config.ts, src/data/sqlite/schema.ts, package.json</read_first>
  <action>Run `npx drizzle-kit push` after schema implementation to align the live local database schema with declared Drizzle tables before verification commands; if command requires interactive input in this environment, rerun with non-interactive flags supported by the configured driver and record exact invocation in summary.</action>
  <acceptance_criteria>
    - Command `npx drizzle-kit push` exits 0.
    - Local database reflects `events` and `sync_checkpoints` tables defined in schema.
  </acceptance_criteria>
  <verify>
    <automated>npx drizzle-kit push</automated>
  </verify>
  <done>Schema push completed successfully and verification can trust runtime schema state.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| app domain → SQLite store | Untrusted input from UI/fixtures crosses into persistent storage. |
| test harness → repository API | Automated replay/persistence commands can mask mutation vulnerabilities if repository surface is too broad. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | T | `src/domain/events/repository.ts` | mitigate | Enforce append/read-only API and avoid update/delete operations for immutable event rows. |
| T-01-02 | R | `src/tests/local-data-backbone.spec.ts` | mitigate | Add explicit assertions for append-only behavior and preserved historical events to keep audit trail verifiable. |
| T-01-03 | D | `src/data/sqlite/client.ts` | accept | Local DB unavailable scenarios are low-scope in phase 1; failures surface through test/runtime errors and are handled in later resilience phases. |
| T-01-04 | I | `src/data/sqlite/schema.ts` | mitigate | Store event payload as structured JSON string and keep access bounded to local app process with no network transport in this phase. |
</threat_model>

<verification>
- `npm run test -- local-data-backbone` passes.
- `npx drizzle-kit push` passes.
</verification>

<success_criteria>
- DATA-01: event data survives simulated DB reopen on same device.
- DATA-02: event history is append-only, with no mutation API path.
</success_criteria>

<output>
After completion, create `.planning/phases/01-local-data-backbone/01-local-data-backbone-01-SUMMARY.md`
</output>
