---
phase: 03-contributor-onboarding-and-authority-model
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/domain/events/types.ts
  - src/domain/projections/types.ts
  - src/domain/projections/replay.ts
  - src/tests/contributor-onboarding-invitations.spec.ts
autonomous: true
requirements:
  - LEDR-03
  - LEDR-04
must_haves:
  truths:
    - "Organizer can generate one-time invitation data for a selected passive participant (LEDR-03)."
    - "Invite can be consumed exactly once and is blocked after consumption or revocation (D-01, D-03, D-04)."
    - "Same participant identity cannot be joined from a second device (D-05, LEDR-04)."
  artifacts:
    - path: "src/domain/events/types.ts"
      provides: "Invite lifecycle event contracts"
      exports: ["KnownEventType", "EventInput", "LedgerEvent"]
    - path: "src/domain/projections/types.ts"
      provides: "Projection fields for invites and contributor-device claims"
      exports: ["LedgerProjection"]
    - path: "src/domain/projections/replay.ts"
      provides: "Deterministic invite lifecycle replay with single-use/single-device guards"
      exports: ["replayLedger"]
    - path: "src/tests/contributor-onboarding-invitations.spec.ts"
      provides: "Automated invite and join invariant tests"
  key_links:
    - from: "src/domain/events/types.ts"
      to: "src/domain/projections/replay.ts"
      via: "eventType switch dispatch for invite.* events"
      pattern: "case \"invite\\.(issued|revoked|consumed)\""
    - from: "src/tests/contributor-onboarding-invitations.spec.ts"
      to: "src/domain/projections/replay.ts"
      via: "replay invariant assertions"
      pattern: "replayLedger\("
---

<objective>
Implement contributor invite lifecycle and one-device join invariants in deterministic replay.

Purpose: Deliver locked onboarding decisions (D-01..D-05) that make invitation and contributor claim behavior enforceable offline from event history.
Output: Extended event/projection contracts, replay logic, and tests covering one-time invite consumption, revoke semantics, and second-device blocking.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-contributor-onboarding-and-authority-model/03-CONTEXT.md
@.planning/phases/03-contributor-onboarding-and-authority-model/03-RESEARCH.md
@.planning/phases/03-contributor-onboarding-and-authority-model/03-PATTERNS.md
@src/domain/events/types.ts
@src/domain/projections/types.ts
@src/domain/projections/replay.ts

<interfaces>
From src/domain/events/types.ts:
```typescript
export type KnownEventType = "ledger.created" | "participant.added" | "expense.created" | "expense.note-added";
export type EventInput = { id: string; ledgerId: string; eventType: EventType; eventVersion: number; occurredAt: string; actorDeviceId: string; payloadJson: string; };
export type LedgerEvent = EventInput & { sequence: number };
```

From src/domain/projections/replay.ts:
```typescript
export function replayLedger(events: LedgerEvent[]): LedgerProjection;
```

From src/domain/projections/types.ts:
```typescript
export type LedgerProjection = {
  ledgerId: string;
  lastSequence: number;
  appliedEventIds: string[];
  entries: LedgerEntry[];
  participants: LedgerParticipant[];
  title: string;
  settlementContext: string;
};
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend onboarding event/projection contracts</name>
  <files>src/domain/events/types.ts, src/domain/projections/types.ts, src/tests/contributor-onboarding-invitations.spec.ts</files>
  <read_first>src/domain/events/types.ts, src/domain/projections/types.ts, src/tests/ledger-setup-participants.spec.ts, .planning/phases/03-contributor-onboarding-and-authority-model/03-CONTEXT.md</read_first>
  <behavior>
    - Test 1: Event type union includes invite.issued, invite.revoked, invite.consumed.
    - Test 2: Projection contract includes invite state and participant→contributor device claim mapping.
    - Test 3: Contract-level test fails until new event names/fields exist.
  </behavior>
  <action>Add explicit onboarding contracts per D-01/D-02/D-03/D-05: in `KnownEventType` include `invite.issued`, `invite.revoked`, `invite.consumed`; add payload types with concrete fields `inviteId`, `participantId`, `inviteCode`, `revokedReason`, `contributorDeviceId`; extend projection types to track invite states (`issued|revoked|consumed`) and participant contributor device claims. Create `src/tests/contributor-onboarding-invitations.spec.ts` with RED tests asserting these literal contract strings exist before implementation.</action>
  <acceptance_criteria>
    - `src/domain/events/types.ts` contains `"invite.issued"`, `"invite.revoked"`, and `"invite.consumed"`.
    - `src/domain/projections/types.ts` contains invite state and contributor device claim fields with explicit string unions.
    - `src/tests/contributor-onboarding-invitations.spec.ts` contains at least one test that references all three invite event names.
  </acceptance_criteria>
  <verify>
    <automated>npm run test -- contributor-onboarding-invitations</automated>
  </verify>
  <done>Onboarding contracts compile and test suite can assert invite lifecycle type surface.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement deterministic invite lifecycle replay invariants</name>
  <files>src/domain/projections/replay.ts, src/tests/contributor-onboarding-invitations.spec.ts</files>
  <read_first>src/domain/projections/replay.ts, src/domain/projections/types.ts, src/tests/contributor-onboarding-invitations.spec.ts, .planning/phases/03-contributor-onboarding-and-authority-model/03-CONTEXT.md</read_first>
  <behavior>
    - Test 1: invite.issued then invite.consumed succeeds once and marks invite consumed.
    - Test 2: invite.consumed after invite.revoked throws a plain-language block message (D-04).
    - Test 3: second invite.consumed for same invite throws deterministic already-used error (D-01).
    - Test 4: consuming invite for participant already claimed by another device throws second-device-block error (D-05, LEDR-04).
  </behavior>
  <action>In replay switch, add handlers for `invite.issued`, `invite.revoked`, and `invite.consumed` using parser functions that validate non-empty string payload fields. Enforce D-01 by rejecting repeat consumption of same inviteId, D-03 by marking revoked invites invalid, D-04 by throwing plain-language errors for used/revoked invite scans instructing contributor to request new code, and D-05/LEDR-04 by rejecting a consume event when `participantId` is already linked to a different `contributorDeviceId`.</action>
  <acceptance_criteria>
    - `src/domain/projections/replay.ts` contains explicit switch branches for all three invite events.
    - replay tests assert error messages matching `(already used|revoked|request a new code|already claimed on another device)`.
    - Replay remains sequence-ordered and still throws `Unsupported eventType` for unknown events.
  </acceptance_criteria>
  <verify>
    <automated>npm run test -- contributor-onboarding-invitations && npm run test -- ledger-setup-participants</automated>
  </verify>
  <done>Invite lifecycle and one-device claim invariants are deterministic, enforced in replay, and regression-safe with earlier phase tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| organizer-generated invite payload → event log | Untrusted or malformed payload values could enter immutable history |
| persisted invite events → replay projection | Event log content drives contributor identity and join authorization |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | T | `src/domain/projections/replay.ts` invite payload parsing | mitigate | Validate invite payload fields as non-empty strings and throw explicit invalid-payload errors before projection mutation. |
| T-03-02 | E | `src/domain/projections/replay.ts` invite consume branch | mitigate | Enforce single-use/single-device checks; reject second consume and cross-device participant claims with deterministic errors. |
| T-03-03 | R | `src/tests/contributor-onboarding-invitations.spec.ts` | mitigate | Add automated tests proving revoked/used invite replays are blocked with explicit messages to prevent silent bypass. |
</threat_model>

<verification>
- `npm run test -- contributor-onboarding-invitations`
- `npm run test -- ledger-setup-participants`
- `npm test`
</verification>

<success_criteria>
- Organizer-side invite lifecycle supports single-use and manual revoke semantics from event history.
- Contributor join attempts are blocked for used/revoked invites and second-device claims.
- All enforced outcomes are deterministic and test-covered.
</success_criteria>

<output>
After completion, create `.planning/phases/03-contributor-onboarding-and-authority-model/03-contributor-onboarding-and-authority-model-01-SUMMARY.md`
</output>
