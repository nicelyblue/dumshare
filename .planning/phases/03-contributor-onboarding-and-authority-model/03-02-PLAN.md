---
phase: 03-contributor-onboarding-and-authority-model
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - src/domain/onboarding/authority.ts
  - src/tests/contributor-authority-policy.spec.ts
autonomous: true
requirements:
  - LEDR-05
must_haves:
  truths:
    - "Organizer is the only accepted sync hub identity in authority checks (D-06)."
    - "Organizer is the only accepted approval authority identity in authority checks (D-06, LEDR-05)."
    - "Non-organizer devices are deterministically rejected with plain-language reasons."
  artifacts:
    - path: "src/domain/onboarding/authority.ts"
      provides: "Pure authority guard helpers for sync-hub and approval checks"
      exports: ["assertOrganizerSyncHub", "assertOrganizerApprovalAuthority"]
    - path: "src/tests/contributor-authority-policy.spec.ts"
      provides: "Automated policy tests for organizer-only authority invariants"
  key_links:
    - from: "src/domain/onboarding/authority.ts"
      to: "src/domain/projections/types.ts"
      via: "projection authority fields consumed by guards"
      pattern: "syncHubDeviceId|approvalAuthorityDeviceId"
    - from: "src/tests/contributor-authority-policy.spec.ts"
      to: "src/domain/onboarding/authority.ts"
      via: "direct allow/deny assertions"
      pattern: "assertOrganizer(SyncHub|ApprovalAuthority)"
---

<objective>
Encode organizer-only authority as reusable domain policy guards.

Purpose: Make D-06 and LEDR-05 enforceable through explicit helper contracts so later sync and approval phases consume a single authority source of truth.
Output: New authority helper module and tests proving organizer-only allow/deny behavior.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-contributor-onboarding-and-authority-model/03-CONTEXT.md
@.planning/phases/03-contributor-onboarding-and-authority-model/03-RESEARCH.md
@src/domain/projections/types.ts
@src/domain/projections/replay.ts

<interfaces>
From src/domain/projections/types.ts (expected after 03-01):
```typescript
export type LedgerProjection = {
  ledgerId: string;
  lastSequence: number;
  appliedEventIds: string[];
  // existing fields...
  syncHubDeviceId: string;
  approvalAuthorityDeviceId: string;
};
```

New module target:
```typescript
export function assertOrganizerSyncHub(projection: LedgerProjection, actorDeviceId: string): void;
export function assertOrganizerApprovalAuthority(projection: LedgerProjection, actorDeviceId: string): void;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create organizer authority guard module</name>
  <files>src/domain/onboarding/authority.ts, src/tests/contributor-authority-policy.spec.ts</files>
  <read_first>src/domain/projections/types.ts, src/domain/projections/replay.ts, src/tests/ledger-setup-participants.spec.ts, .planning/phases/03-contributor-onboarding-and-authority-model/03-CONTEXT.md</read_first>
  <behavior>
    - Test 1: organizer actorDeviceId passes sync hub guard.
    - Test 2: non-organizer actorDeviceId throws organizer-only sync hub error.
    - Test 3: organizer actorDeviceId passes approval authority guard.
    - Test 4: non-organizer actorDeviceId throws organizer-only approval error.
  </behavior>
  <action>Create `src/domain/onboarding/authority.ts` implementing `assertOrganizerSyncHub` and `assertOrganizerApprovalAuthority` as pure functions that compare `actorDeviceId` to projection authority fields. Enforce D-06 by throwing explicit plain-language errors like `Only organizer device can run sync hub actions` and `Only organizer device can approve contributor submissions` when actor differs.</action>
  <acceptance_criteria>
    - `src/domain/onboarding/authority.ts` exports both guard functions with `LedgerProjection` input.
    - `src/tests/contributor-authority-policy.spec.ts` contains pass + fail cases for both guards.
    - Fail-path assertions match organizer-only error strings.
  </acceptance_criteria>
  <verify>
    <automated>npm run test -- contributor-authority-policy</automated>
  </verify>
  <done>Organizer-only guard functions are implemented and fully covered by deterministic tests.</done>
</task>

<task type="auto">
  <name>Task 2: Verify authority guards against replay-derived state and full suite</name>
  <files>src/tests/contributor-authority-policy.spec.ts</files>
  <read_first>src/tests/contributor-authority-policy.spec.ts, src/domain/projections/replay.ts, src/domain/onboarding/authority.ts</read_first>
  <action>Add test fixtures that build projection state from replayed events (rather than hand-crafted objects only) and run guard checks to confirm wiring remains compatible with deterministic replay outputs. Include both organizer actor and non-organizer actor scenarios for sync and approval authority flows.</action>
  <acceptance_criteria>
    - Test file includes at least one scenario using `replayLedger(...)` output as guard input.
    - Full test run passes with no regressions in prior phase suites.
  </acceptance_criteria>
  <verify>
    <automated>npm run test -- contributor-authority-policy && npm test</automated>
  </verify>
  <done>Authority checks are proven both in unit-style fixtures and replay-integrated scenarios.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| actor device ID input → authority guard | Untrusted caller identity is evaluated for privileged organizer actions |
| replay projection authority fields → guard module | Guard correctness depends on deterministic authority fields in projection |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-04 | E | `src/domain/onboarding/authority.ts` privilege checks | mitigate | Centralize organizer-only checks in explicit guard helpers that throw on non-organizer actor IDs. |
| T-03-05 | S | `src/domain/onboarding/authority.ts` actor identity comparison | mitigate | Compare actor ID against replay-derived authority fields; reject mismatches with deterministic errors. |
| T-03-06 | D | `src/tests/contributor-authority-policy.spec.ts` integration coverage | mitigate | Add replay-backed test fixtures to ensure guard behavior remains valid against actual projection outputs. |
</threat_model>

<verification>
- `npm run test -- contributor-authority-policy`
- `npm test`
</verification>

<success_criteria>
- Organizer-only authority checks exist as reusable domain guards.
- Non-organizer devices are rejected for sync-hub and approval-authority operations.
- Guard behavior is test-proven with replay-derived projection input.
</success_criteria>

<output>
After completion, create `.planning/phases/03-contributor-onboarding-and-authority-model/03-contributor-onboarding-and-authority-model-02-SUMMARY.md`
</output>
