---
phase: 05-split-modes-and-contributor-amendments
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/domain/events/types.ts
  - src/domain/projections/types.ts
  - src/domain/projections/replay.ts
  - src/tests/expense-split-modes.spec.ts
autonomous: true
requirements:
  - EXPS-04
  - EXPS-05
  - EXPS-06
must_haves:
  truths:
    - "User can split an expense equally across selected participants and replay derives deterministic owed shares (EXPS-04)."
    - "User can split an expense by exact owed amounts and replay rejects totals that do not match the expense total (EXPS-05)."
    - "User can split an expense by percentages and replay derives deterministic owed shares that sum exactly to totalAmountMinor (EXPS-06)."
  artifacts:
    - path: "src/domain/events/types.ts"
      provides: "Split-mode payload contracts attached to expense.created"
      exports: ["KnownEventType", "ExpenseCreatedPayload", "LedgerEvent"]
    - path: "src/domain/projections/types.ts"
      provides: "Entry-level split definition and computed owedShares"
      exports: ["LedgerEntry", "LedgerProjection"]
    - path: "src/domain/projections/replay.ts"
      provides: "Deterministic split validation and owed-share derivation for equal/exact/percentage"
      exports: ["replayLedger"]
    - path: "src/tests/expense-split-modes.spec.ts"
      provides: "Automated split-mode contract + replay invariants for EXPS-04/05/06"
  key_links:
    - from: "src/domain/events/types.ts"
      to: "src/domain/projections/replay.ts"
      via: "parseExpenseCreatedPayload + split validation helpers"
      pattern: "split\.mode|derive.*Split|case \"expense.created\""
    - from: "src/domain/projections/replay.ts"
      to: "src/domain/projections/types.ts"
      via: "projection.entries push with split and owedShares"
      pattern: "projection\.entries\.push"
    - from: "src/tests/expense-split-modes.spec.ts"
      to: "src/domain/projections/replay.ts"
      via: "replayLedger split mode success/failure assertions"
      pattern: "replayLedger\("
---

<objective>
Implement deterministic equal/exact/percentage expense split modes in event contracts and replay projection.

Purpose: Deliver EXPS-04/05/06 with strict, replay-safe split validation and deterministic owed-share outputs required for later balance calculation.
Output: Updated split-aware expense contracts, replay split derivation logic, and automated split-mode tests.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-split-modes-and-contributor-amendments/05-RESEARCH.md
@.planning/phases/05-split-modes-and-contributor-amendments/05-PATTERNS.md
@src/domain/events/types.ts
@src/domain/projections/types.ts
@src/domain/projections/replay.ts

<interfaces>
From src/domain/events/types.ts:
```typescript
export type ExpenseCreatedPayload = {
  expenseId: string;
  description: string;
  currency: string;
  totalAmountMinor: number;
  expenseDate: string;
  creatorRole: "organizer" | "contributor";
  payers: { participantId: string; paidAmountMinor: number }[];
};
```

From src/domain/projections/types.ts:
```typescript
export type LedgerEntry = {
  expenseId: string;
  totalAmountMinor: number;
  creatorRole: "organizer" | "contributor";
  payers: { participantId: string; paidAmountMinor: number }[];
};
```

From src/domain/projections/replay.ts:
```typescript
export function replayLedger(events: LedgerEvent[]): LedgerProjection;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend split-mode contracts and write failing split invariants</name>
  <files>src/domain/events/types.ts, src/domain/projections/types.ts, src/tests/expense-split-modes.spec.ts</files>
  <read_first>src/domain/events/types.ts, src/domain/projections/types.ts, src/tests/expense-capture-foundations.spec.ts, .planning/phases/05-split-modes-and-contributor-amendments/05-RESEARCH.md</read_first>
  <behavior>
    - Test 1: Contract surface includes split mode union `equal|exact|percentage` under `expense.created`.
    - Test 2: Projection contract includes persisted split definition and computed `owedShares` rows.
    - Test 3: Spec contains failing replay expectations for invalid exact/percentage totals before replay implementation.
  </behavior>
  <action>Update `src/domain/events/types.ts` to add strict split payload shapes: `EqualSplitPayload`, `ExactSplitPayload`, and `PercentageSplitPayload`, then include `split` on `ExpenseCreatedPayload` with `mode` and participant rows (percentage expressed as `percentageBps`). Update `src/domain/projections/types.ts` to add `split` and `owedShares` onto `LedgerEntry`. Create `src/tests/expense-split-modes.spec.ts` with contract assertions and replay tests covering equal split, exact mismatch rejection, and percentage sum rejection for EXPS-04/05/06.</action>
  <acceptance_criteria>
    - `src/domain/events/types.ts` contains `mode: "equal" | "exact" | "percentage"` and `percentageBps`.
    - `src/domain/projections/types.ts` contains `owedShares` and split metadata on `LedgerEntry`.
    - `src/tests/expense-split-modes.spec.ts` contains tests referencing EXPS-04, EXPS-05, and EXPS-06.
  </acceptance_criteria>
  <verify>
    <automated>npm run test -- expense-split-modes</automated>
  </verify>
  <done>Split-mode contracts and projection fields are explicit and test-addressable for all three phase-required split modes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement deterministic split validation and owed-share derivation</name>
  <files>src/domain/projections/replay.ts, src/tests/expense-split-modes.spec.ts</files>
  <read_first>src/domain/projections/replay.ts, src/domain/events/types.ts, src/domain/projections/types.ts, src/tests/expense-split-modes.spec.ts, src/tests/expense-capture-foundations.spec.ts</read_first>
  <behavior>
    - Test 1: Equal split derives deterministic owed shares across selected participants and sums to `totalAmountMinor`.
    - Test 2: Exact split requires owed share sum to equal `totalAmountMinor`, otherwise replay throws deterministic error.
    - Test 3: Percentage split requires total `percentageBps` exactly 10000 and derived owed shares sum to `totalAmountMinor`.
    - Test 4: Unknown split participant IDs are rejected before entry append.
  </behavior>
  <action>In `src/domain/projections/replay.ts`, add split parser/validator helpers called by the `expense.created` branch. Enforce unique participant IDs and participant existence in roster. Implement deterministic owed-share derivation: equal split uses integer division with stable remainder assignment in listed participant order; exact split uses provided owed amounts and enforces sum match; percentage split computes owed amounts from basis points and applies deterministic remainder distribution so final owed sum equals `totalAmountMinor`. Persist both split definition and computed `owedShares` into `projection.entries`.</action>
  <acceptance_criteria>
    - Replay branch for `expense.created` validates split mode and participant rows before mutating projection.
    - Equal/exact/percentage owed shares each sum exactly to `totalAmountMinor` for valid payloads.
    - Error strings include deterministic guidance for invalid exact total, invalid percentage total, and unknown split participant.
  </acceptance_criteria>
  <verify>
    <automated>npm run test -- expense-split-modes && npm run test -- expense-capture-foundations && npm test</automated>
  </verify>
  <done>Split-mode replay behavior is deterministic, integrity-safe, and fully verified for EXPS-04/05/06 without regression.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| split payload JSON -> replay parser | Untrusted split payload values cross into deterministic projection logic |
| split participant IDs -> roster lookup | Foreign participant references can corrupt owed-share integrity |
| arithmetic derivation -> ledger entry persistence | Non-deterministic or drifting calculations can poison downstream balances |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | T | `src/domain/projections/replay.ts` split parser | mitigate | Validate mode-specific required fields, uniqueness, positivity, and integer constraints before any state mutation. |
| T-05-02 | I | `src/domain/projections/replay.ts` participant validation | mitigate | Enforce split participant IDs exist in `projection.participants`; reject unknown IDs with deterministic error. |
| T-05-03 | D | `src/domain/projections/replay.ts` owed-share derivation | mitigate | Use integer-only deterministic arithmetic with explicit remainder assignment and final sum checks. |
| T-05-04 | R | `src/tests/expense-split-modes.spec.ts` | mitigate | Add invariant tests for each split mode and invalid branch to prevent silent contract drift. |
</threat_model>

<verification>
- `npm run test -- expense-split-modes`
- `npm run test -- expense-capture-foundations`
- `npm test`
</verification>

<success_criteria>
- EXPS-04: Equal split inputs are accepted and produce deterministic owed shares.
- EXPS-05: Exact split inputs are accepted only when exact owed totals match the expense total.
- EXPS-06: Percentage split inputs are accepted only when percentage totals equal 100% (10000 bps) and derived owed shares sum to total.
</success_criteria>

<output>
After completion, create `.planning/phases/05-split-modes-and-contributor-amendments/05-split-modes-and-contributor-amendments-01-SUMMARY.md`
</output>
