---
phase: 05-split-modes-and-contributor-amendments
plan: 02
type: execute
wave: 2
depends_on:
  - 05-01
files_modified:
  - src/domain/events/types.ts
  - src/domain/projections/types.ts
  - src/domain/projections/replay.ts
  - src/tests/contributor-amendments.spec.ts
autonomous: true
requirements:
  - EXPS-07
must_haves:
  truths:
    - "Contributor can submit an amendment against an existing submitted expense (EXPS-07)."
    - "Contributor amendment is treated as a reviewable submission and does not auto-apply to approved entries (EXPS-07)."
    - "Only claimed contributor devices can submit contributor amendments, preserving organizer-governed flow (EXPS-07)."
  artifacts:
    - path: "src/domain/events/types.ts"
      provides: "Typed amendment-submission event payload contract"
      exports: ["KnownEventType", "ExpenseAmendmentSubmittedPayload"]
    - path: "src/domain/projections/types.ts"
      provides: "Pending contributor submission projection shape for amendment review pipeline"
      exports: ["LedgerProjection"]
    - path: "src/domain/projections/replay.ts"
      provides: "Replay branch that validates and queues contributor amendments without mutating approved entries"
      exports: ["replayLedger"]
    - path: "src/tests/contributor-amendments.spec.ts"
      provides: "Automated EXPS-07 coverage for amendment queueing and guard invariants"
  key_links:
    - from: "src/domain/events/types.ts"
      to: "src/domain/projections/replay.ts"
      via: "expense.amendment-submitted parser + branch"
      pattern: "expense\.amendment-submitted|parse.*Amendment"
    - from: "src/domain/projections/replay.ts"
      to: "src/domain/projections/types.ts"
      via: "pendingSubmissions append without projection.entries mutation"
      pattern: "pendingSubmissions\.push"
    - from: "src/tests/contributor-amendments.spec.ts"
      to: "src/domain/projections/replay.ts"
      via: "replay assertions for authorization, queueing, and non-auto-apply"
      pattern: "replayLedger\("
---

<objective>
Implement contributor expense amendment submission as a deterministic pending-review feed, not direct approved-state mutation.

Purpose: Satisfy EXPS-07 and prepare Phase 6 organizer approval gate by establishing strict amendment submission contracts and replay behavior.
Output: New amendment event contract, pending-submission projection state, replay guard logic, and automated amendment tests.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-split-modes-and-contributor-amendments/05-RESEARCH.md
@.planning/phases/05-split-modes-and-contributor-amendments/05-01-PLAN.md
@src/domain/events/types.ts
@src/domain/projections/types.ts
@src/domain/projections/replay.ts

<interfaces>
From src/domain/projections/types.ts:
```typescript
export type LedgerProjection = {
  entries: LedgerEntry[];
  participants: LedgerParticipant[];
  participantContributorDeviceClaims: Record<string, string>;
};
```

From src/domain/projections/replay.ts:
```typescript
function isExpenseCreatorAuthorized(projection: LedgerProjection, actorDeviceId: string): boolean;
export function replayLedger(events: LedgerEvent[]): LedgerProjection;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add amendment submission contracts and failing pending-flow tests</name>
  <files>src/domain/events/types.ts, src/domain/projections/types.ts, src/tests/contributor-amendments.spec.ts</files>
  <read_first>src/domain/events/types.ts, src/domain/projections/types.ts, src/tests/contributor-onboarding-invitations.spec.ts, src/tests/expense-split-modes.spec.ts</read_first>
  <behavior>
    - Test 1: Contract surface exposes `expense.amendment-submitted` event type and typed payload fields.
    - Test 2: Projection contract exposes pending contributor submission collection for review queueing.
    - Test 3: Spec includes failing replay expectations proving amendments should not auto-edit approved entries.
  </behavior>
  <action>Extend `src/domain/events/types.ts` with `expense.amendment-submitted` in `KnownEventType` and a strict `ExpenseAmendmentSubmittedPayload` that includes `amendmentId`, `targetExpenseId`, `reason`, and `proposedExpense` (same split-aware shape introduced in Plan 01). Extend `src/domain/projections/types.ts` with a `pendingSubmissions` collection containing amendment metadata and proposed expense payload. Create `src/tests/contributor-amendments.spec.ts` with contract assertions and replay invariants for EXPS-07, including a failing expectation that approved `entries` remain unchanged after amendment submission.</action>
  <acceptance_criteria>
    - `src/domain/events/types.ts` includes `"expense.amendment-submitted"` and `ExpenseAmendmentSubmittedPayload`.
    - `src/domain/projections/types.ts` includes `pendingSubmissions` with amendment submission shape.
    - `src/tests/contributor-amendments.spec.ts` contains explicit EXPS-07 assertions and non-auto-apply expectations.
  </acceptance_criteria>
  <verify>
    <automated>npm run test -- contributor-amendments</automated>
  </verify>
  <done>Amendment submission contracts and pending-queue projection fields are explicit, and failing tests define EXPS-07 behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement replay queueing for contributor amendments with authority and target guards</name>
  <files>src/domain/projections/replay.ts, src/tests/contributor-amendments.spec.ts</files>
  <read_first>src/domain/projections/replay.ts, src/domain/events/types.ts, src/domain/projections/types.ts, src/tests/contributor-amendments.spec.ts, src/tests/contributor-authority-policy.spec.ts</read_first>
  <behavior>
    - Test 1: Claimed contributor device can submit amendment for an existing target expense, and replay appends to `pendingSubmissions`.
    - Test 2: Unclaimed/unknown contributor device is rejected with deterministic plain-language authorization error.
    - Test 3: Amendment referencing unknown target expense is rejected.
    - Test 4: Amendment submission does not mutate `projection.entries` until future organizer approval flow.
  </behavior>
  <action>In `src/domain/projections/replay.ts`, add parser + branch for `expense.amendment-submitted`. Reuse split validation for `proposedExpense` payload to avoid duplicated acceptance rules. Enforce actor authorization by requiring `event.actorDeviceId` to exist in `projection.participantContributorDeviceClaims` values and reject otherwise. Enforce `targetExpenseId` exists in `projection.entries`. On success, append amendment record into `projection.pendingSubmissions`; do not alter existing `projection.entries`. Preserve strict fail-fast behavior and deterministic error strings for unauthorized actor and unknown target expense.</action>
  <acceptance_criteria>
    - Replay includes a dedicated `expense.amendment-submitted` switch branch with strict parser usage.
    - Successful amendment submissions increase `pendingSubmissions` length and keep `entries` length unchanged.
    - Unauthorized actor and missing target expense both throw deterministic plain-language errors.
  </acceptance_criteria>
  <verify>
    <automated>npm run test -- contributor-amendments && npm run test -- expense-split-modes && npm test</automated>
  </verify>
  <done>Contributor amendments are replay-validated reviewable submissions queued for organizer decision, satisfying EXPS-07 without auto-applying approved state.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| contributor amendment payload -> replay parser | Untrusted amendment data enters approval pipeline state |
| actorDeviceId -> amendment authority check | Spoofed device identity could inject fraudulent amendment submissions |
| targetExpenseId -> approved entry lookup | Nonexistent target references can corrupt review flow integrity |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-05 | S | `src/domain/projections/replay.ts` amendment authority guard | mitigate | Require actor device to match replay-derived claimed contributor mapping; reject unknown actors. |
| T-05-06 | T | `src/domain/projections/replay.ts` amendment parser | mitigate | Validate all amendment payload fields and nested proposed expense shape before queue mutation. |
| T-05-07 | I | `src/domain/projections/replay.ts` target-expense validation | mitigate | Require amendment target exists in approved entries before pending queue append. |
| T-05-08 | R | `src/tests/contributor-amendments.spec.ts` | mitigate | Add deterministic tests proving queueing behavior and non-auto-apply invariants for EXPS-07. |
</threat_model>

<verification>
- `npm run test -- contributor-amendments`
- `npm run test -- expense-split-modes`
- `npm test`
</verification>

<success_criteria>
- EXPS-07: Contributor amendments can be submitted against existing expenses and are represented as reviewable pending submissions.
- Amendment submissions do not directly mutate approved ledger entries.
- Replay enforces claimed-contributor authority and target-expense existence before queueing.
</success_criteria>

<output>
After completion, create `.planning/phases/05-split-modes-and-contributor-amendments/05-split-modes-and-contributor-amendments-02-SUMMARY.md`
</output>
