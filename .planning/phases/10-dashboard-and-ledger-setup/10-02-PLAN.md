---
phase: 10-dashboard-and-ledger-setup
plan: 02
type: execute
wave: 2
depends_on:
  - 10-01
files_modified:
  - src/data/ledger/ledgerMutations.ts
  - src/state/ledgerSession.tsx
  - src/screens/LedgerSetupScreen.tsx
  - src/screens/FeatureScreen.tsx
  - src/ui/LabeledField.tsx
autonomous: true
requirements:
  - FE-04
must_haves:
  truths:
    - "Organizer can create or edit ledger setup data through a mobile form flow."
    - "Organizer can manage the participant roster from the app UI without needing to inspect raw data structures."
    - "Setup changes are expressed as domain events and reflected through the same replay-backed session used by the dashboard."
  artifacts:
    - path: "src/data/ledger/ledgerMutations.ts"
      provides: "Ledger setup mutation helpers that append create/update participant events"
      exports: ["createLedgerSetupMutations"]
    - path: "src/state/ledgerSession.tsx"
      provides: "Shared session actions for saving ledger title, settlement context, and roster updates"
      exports: ["LedgerSessionProvider", "useLedgerSession"]
    - path: "src/screens/LedgerSetupScreen.tsx"
      provides: "Organizer form flow for ledger title, settlement context, and roster editing"
      exports: ["LedgerSetupScreen"]
    - path: "src/ui/LabeledField.tsx"
      provides: "Reusable labeled form field for setup inputs"
      exports: ["LabeledField"]
    - path: "src/screens/FeatureScreen.tsx"
      provides: "Route dispatcher that renders the ledger setup screen for the setup destination"
      exports: ["FeatureScreen"]
  key_links:
    - from: "src/data/ledger/ledgerMutations.ts"
      to: "src/domain/events/repository.ts"
      via: "appendEvent for ledger.created and participant.added events"
      pattern: "appendEvent"
    - from: "src/state/ledgerSession.tsx"
      to: "src/data/ledger/ledgerMutations.ts"
      via: "mutation actions exposed through the session hook"
      pattern: "useLedgerSession"
    - from: "src/screens/LedgerSetupScreen.tsx"
      to: "src/ui/LabeledField.tsx"
      via: "form inputs and edit controls"
      pattern: "LabeledField"
---

<objective>
Deliver the ledger setup slice of Phase 10: organizer form flows for ledger metadata and participant roster management, backed by replay-safe domain events.

Purpose: Satisfy FE-04 by letting the organizer create and edit ledger setup data from mobile screens that stay aligned with the validated domain model.
Output: Ledger setup mutation helpers, shared session save actions, branded setup screen, labeled form field primitives, and route dispatch wiring.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-dashboard-and-ledger-setup/10-01-SUMMARY.md
@src/domain/events/repository.ts
@src/domain/events/types.ts
@src/domain/projections/index.ts
@src/screens/FeatureScreen.tsx
@src/state/ledgerSession.tsx

<interfaces>
From `src/domain/events/repository.ts`:
```typescript
export type EventRepository = {
  appendEvent(input: AppendEventInput): Promise<void>;
  listEventsByLedger(ledgerId: string): Promise<StoredEvent[]>;
};
```

From `src/domain/events/types.ts`:
```typescript
export type LedgerCreatedPayload = {
  title: string;
  settlementContext: string;
};

export type ParticipantAddedPayload = {
  participantId: string;
  displayName: string;
};
```

From `src/state/ledgerSession.tsx`:
```typescript
export function useLedgerSession(): LedgerSessionValue;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add ledger setup mutation helpers and session save actions</name>
  <files>src/data/ledger/ledgerMutations.ts, src/state/ledgerSession.tsx</files>
  <action>Create a small mutation layer that turns ledger setup form input into domain events: creating the ledger metadata, updating title or settlement context, and appending participant rows for roster changes. Expose these mutations through the shared ledger session hook so the setup screen can save changes without reaching into SQLite directly. Keep the mutation contract replay-safe by reusing the same payload field names and validation rules enforced by the domain event types.</action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>The session layer can persist setup changes as domain events and remains consistent with the replay model.</done>
</task>

<task type="auto">
  <name>Task 2: Build the organizer setup screen and participant roster editor</name>
  <files>src/screens/LedgerSetupScreen.tsx, src/ui/LabeledField.tsx, src/screens/FeatureScreen.tsx, src/navigation/featureRegistry.ts</files>
  <action>Build a branded setup screen inside `AppShell` with labeled fields for ledger title and settlement context plus a participant roster editor that can add and rename names without exposing raw event structures. Use `LabeledField` for consistent form chrome and keep the roster UI plain-language and mobile-friendly. Update the feature dispatcher so the setup route renders this screen, while preserving the shared shell and sibling navigation behavior already established in Phase 9.</action>
  <verify>
    <automated>npx tsc --noEmit && npm test</automated>
  </verify>
  <done>The organizer can create or edit ledger setup data and manage the participant roster from the app UI.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| form input -> ledger mutation helpers | Organizer edits become domain events only after validation and explicit save |
| participant roster UI -> event append layer | Roster changes must not bypass the event log or reveal raw storage details |
| setup route tap -> screen state | Navigation should remain within known shell destinations and not leak out of the app shell |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-04 | T | `src/data/ledger/ledgerMutations.ts` | mitigate | Convert form values into typed ledger events and validate required fields before appendEvent calls. |
| T-10-05 | I | `src/screens/LedgerSetupScreen.tsx` | mitigate | Render only friendly labels and event-backed state so roster and setup details stay understandable and non-technical. |
| T-10-06 | D | `src/state/ledgerSession.tsx` | mitigate | Keep save actions bounded to explicit organizer input and avoid hidden background writes. |
</threat_model>

<verification>
- `npx tsc --noEmit`
- `npm test`
</verification>

<success_criteria>
- FE-04 is satisfied: organizer can create/edit ledger setup data and manage the participant roster from mobile screens.
- Setup changes flow through typed ledger events and the shared replay-backed session.
- The setup screen remains inside the branded app shell and preserves route navigation.
</success_criteria>

<output>
After completion, create `.planning/phases/10-dashboard-and-ledger-setup/10-02-SUMMARY.md`
</output>