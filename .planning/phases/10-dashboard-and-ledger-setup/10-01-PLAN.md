---
phase: 10-dashboard-and-ledger-setup
plan: 01
type: execute
wave: 1
depends_on:
  - 09-02
files_modified:
  - src/data/ledger/ledgerSnapshot.ts
  - src/state/ledgerSession.tsx
  - src/screens/DashboardScreen.tsx
  - src/screens/FeatureScreen.tsx
  - src/ui/SummaryCard.tsx
autonomous: true
requirements:
  - FE-03
must_haves:
  truths:
    - "User can see a dashboard summary that includes the current ledger title, participant count, pending approvals, latest activity, and a per-currency balance snapshot."
    - "The dashboard renders from a projection loader that replays local events rather than duplicating ledger math in the UI."
    - "The dashboard stays inside the branded app shell and still exposes navigation to the other main frontend areas."
  artifacts:
    - path: "src/data/ledger/ledgerSnapshot.ts"
      provides: "Ledger snapshot loader that reads repository events and builds dashboard view models"
      exports: ["loadLedgerDashboardSnapshot", "LedgerDashboardSnapshot"]
    - path: "src/state/ledgerSession.tsx"
      provides: "Shared ledger session/provider for loading and refreshing the current projection"
      exports: ["LedgerSessionProvider", "useLedgerSession"]
    - path: "src/screens/DashboardScreen.tsx"
      provides: "Branded dashboard screen with summary cards and empty/loading/error states"
      exports: ["DashboardScreen"]
    - path: "src/ui/SummaryCard.tsx"
      provides: "Reusable metric card for dashboard ledger status"
      exports: ["SummaryCard"]
    - path: "src/screens/FeatureScreen.tsx"
      provides: "Route dispatcher that renders the dashboard screen for the dashboard destination"
      exports: ["FeatureScreen"]
  key_links:
    - from: "src/data/ledger/ledgerSnapshot.ts"
      to: "src/domain/events/repository.ts"
      via: "listEventsByLedger and replayLedger"
      pattern: "listEventsByLedger|replayLedger"
    - from: "src/data/ledger/ledgerSnapshot.ts"
      to: "src/domain/balances/index.ts"
      via: "buildApprovedBalanceSummary for per-currency snapshot rows"
      pattern: "buildApprovedBalanceSummary"
    - from: "src/screens/DashboardScreen.tsx"
      to: "src/state/ledgerSession.tsx"
      via: "hook-driven loading, refresh, and projection state"
      pattern: "useLedgerSession"
---

<objective>
Deliver the dashboard slice of Phase 10: a live summary screen that reads the replayed ledger projection and presents ledger status, participant count, pending approvals, and per-currency balances inside the existing shell.

Purpose: Satisfy FE-03 by exposing current ledger health through a thin projection-backed dashboard instead of static placeholder content.
Output: Dashboard projection loader, shared ledger session hook, branded dashboard screen, and reusable summary cards.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-app-shell-and-navigation/09-02-SUMMARY.md
@src/domain/events/repository.ts
@src/domain/projections/index.ts
@src/domain/balances/index.ts
@src/navigation/featureRegistry.ts
@src/screens/FeatureScreen.tsx

<interfaces>
From `src/domain/events/repository.ts`:
```typescript
export type EventRepository = {
  appendEvent(input: AppendEventInput): Promise<void>;
  listEventsByLedger(ledgerId: string): Promise<StoredEvent[]>;
};
```

From `src/domain/balances/index.ts`:
```typescript
export function buildApprovedBalanceSummary(projection: LedgerProjection): ApprovedBalanceSummary;
```

From `src/screens/FeatureScreen.tsx`:
```typescript
export function FeatureScreen({ feature, onNavigate }: FeatureScreenProps): JSX.Element;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the ledger snapshot loader and session provider for dashboard state</name>
  <files>src/data/ledger/ledgerSnapshot.ts, src/state/ledgerSession.tsx</files>
  <action>Create a small dashboard-oriented data loader that opens the local event repository, replays the current ledger events, and derives a view model with ledger title, participant count, pending approval count, latest activity label, and per-currency balance summary. Wrap it in a `LedgerSessionProvider`/`useLedgerSession` hook pair so dashboard and setup screens can consume one shared projection source without re-implementing replay logic. Keep loading, empty-ledger, and error states explicit so the UI never has to guess whether data exists.</action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>The app has a single replay-backed ledger session source for dashboard rendering and future setup mutations.</done>
</task>

<task type="auto">
  <name>Task 2: Build the branded dashboard screen and route dispatch wiring</name>
  <files>src/screens/DashboardScreen.tsx, src/ui/SummaryCard.tsx, src/screens/FeatureScreen.tsx, src/navigation/featureRegistry.ts</files>
  <action>Build a dashboard screen inside `AppShell` that renders summary cards for ledger title, participant count, pending approvals, latest activity, and per-currency balance rows from the session hook. Add a reusable `SummaryCard` for consistent metric presentation. Update the feature dispatcher so the dashboard route renders this screen while the other routes continue to use the existing shell navigation pattern. Keep the dashboard layout distinct and high-contrast, but do not duplicate projection math in the component tree.</action>
  <verify>
    <automated>npx tsc --noEmit && npm test</automated>
  </verify>
  <done>The dashboard route shows live ledger status in the shell and remains navigable to the other main areas.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SQLite events -> dashboard snapshot loader | Raw ledger history must be replayed into a safe read model before display |
| projection summary -> user-facing status cards | Derived summary data must not expose stale or miscomputed financial state |
| dashboard route tap -> cross-screen navigation | Navigation should only move between known app destinations inside the shell |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01 | T | `src/data/ledger/ledgerSnapshot.ts` | mitigate | Replay local events and derive the dashboard view model from typed projection outputs instead of reading raw event payloads in UI code. |
| T-10-02 | I | `src/screens/DashboardScreen.tsx` | mitigate | Render only replay-derived ledger summary data and show explicit loading/empty/error states so users do not misread partial data. |
| T-10-03 | D | `src/state/ledgerSession.tsx` | mitigate | Keep loading and refresh paths bounded to the selected ledger projection and avoid unbounded work on render. |
</threat_model>

<verification>
- `npx tsc --noEmit`
- `npm test`
</verification>

<success_criteria>
- FE-03 is satisfied: the dashboard shows ledger title, participant count, pending approvals, latest activity, and per-currency balance snapshot.
- Dashboard data comes from a replay-backed session loader, not duplicated UI math.
- The shell remains intact and navigation to other primary areas is preserved.
</success_criteria>

<output>
After completion, create `.planning/phases/10-dashboard-and-ledger-setup/10-01-SUMMARY.md`
</output>