---
phase: 11-expense-entry-and-review-ui
plan: 01
type: execute
wave: 1
depends_on:
  - 10-02
files_modified:
  - src/data/ledger/expenseDrafts.ts
  - src/state/ledgerSession.tsx
  - src/screens/ExpenseEntryScreen.tsx
  - src/ui/ExpenseSplitEditor.tsx
  - src/screens/FeatureScreen.tsx
  - src/navigation/featureRegistry.ts
autonomous: true
requirements:
  - FE-05
must_haves:
  truths:
    - "User can draft an expense with payer rows and equal, exact, or percentage split controls."
    - "Submitting the draft writes a domain event through the shared ledger session instead of bypassing replay-safe persistence."
    - "The expense entry flow stays inside the branded shell and still reaches the other main app areas."
  artifacts:
    - path: "src/data/ledger/expenseDrafts.ts"
      provides: "Expense draft normalization helpers and event payload builders"
      exports: ["buildExpenseCreatedEventInput", "normalizeExpenseDraft"]
    - path: "src/state/ledgerSession.tsx"
      provides: "Shared session submit action for organizer/contributor expense drafts"
      exports: ["LedgerSessionProvider", "useLedgerSession"]
    - path: "src/screens/ExpenseEntryScreen.tsx"
      provides: "Branded mobile form for expense description, currency, payers, and split mode"
      exports: ["ExpenseEntryScreen"]
    - path: "src/ui/ExpenseSplitEditor.tsx"
      provides: "Reusable split-mode editor and payer row controls"
      exports: ["ExpenseSplitEditor"]
    - path: "src/screens/FeatureScreen.tsx"
      provides: "Route dispatcher that renders the expense entry screen for the expense destination"
      exports: ["FeatureScreen"]
  key_links:
    - from: "src/data/ledger/expenseDrafts.ts"
      to: "src/domain/events/types.ts"
      via: "ExpenseCreatedPayload, EqualSplitPayload, ExactSplitPayload, PercentageSplitPayload"
      pattern: "ExpenseCreatedPayload|ExpenseSplitPayload"
    - from: "src/state/ledgerSession.tsx"
      to: "src/data/ledger/expenseDrafts.ts"
      via: "submitExpenseDraft action"
      pattern: "submitExpenseDraft"
    - from: "src/screens/ExpenseEntryScreen.tsx"
      to: "src/ui/ExpenseSplitEditor.tsx"
      via: "split-mode picker and payer row composition"
      pattern: "ExpenseSplitEditor"
---

<objective>
Deliver the expense-entry slice of Phase 11: a mobile expense draft form that captures payer rows and split controls, then saves the draft as a replay-safe domain event through the shared session.

Purpose: Satisfy FE-05 by letting the user draft organizer or contributor expenses with equal, exact, or percentage splits without duplicating domain validation in the UI.
Output: Expense draft normalization helpers, shared submit action, branded expense entry screen, reusable split editor controls, and shell route dispatch wiring.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-dashboard-and-ledger-setup/10-02-SUMMARY.md
@src/domain/events/types.ts
@src/domain/projections/types.ts
@src/domain/projections/replay.ts
@src/domain/events/repository.ts
@src/state/ledgerSession.tsx
@src/navigation/featureRegistry.ts
@src/screens/FeatureScreen.tsx

<interfaces>
From `src/domain/events/types.ts`:
```typescript
export type ExpenseCreatedPayload = {
  expenseId: string;
  description: string;
  currency: string;
  totalAmountMinor: number;
  expenseDate: string;
  creatorRole: "organizer" | "contributor";
  payers: { participantId: string; paidAmountMinor: number }[];
  split: ExpenseSplitPayload;
};
```

From `src/domain/projections/types.ts`:
```typescript
export type LedgerProjection = {
  participants: LedgerParticipant[];
  entries: LedgerEntry[];
  pendingSubmissions: unknown[];
};
```

From `src/state/ledgerSession.tsx`:
```typescript
export function useLedgerSession(): LedgerSessionValue;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add expense draft normalization and session submit action</name>
  <files>src/data/ledger/expenseDrafts.ts, src/state/ledgerSession.tsx</files>
  <action>Create a small expense-draft helper that turns form state into a strict `expense.created` event payload using the existing expense split contract and payer-row validation rules. Extend the shared ledger session with a `submitExpenseDraft` action that appends the event through the repository, refreshes the replay-backed snapshot, and returns the new event ID or expense ID for UI feedback. Keep validation language aligned with the domain layer so the screen reports the same errors the replay path uses.</action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>The session can accept expense drafts as domain events and the draft helper preserves the existing replay contract.</done>
</task>

<task type="auto">
  <name>Task 2: Build the branded expense entry screen and split editor</name>
  <files>src/screens/ExpenseEntryScreen.tsx, src/ui/ExpenseSplitEditor.tsx, src/screens/FeatureScreen.tsx, src/navigation/featureRegistry.ts</files>
  <action>Build a branded expense-entry screen inside `AppShell` with fields for description, currency, expense date, payer rows, and a split editor that supports equal, exact, and percentage modes. Use `ExpenseSplitEditor` for the payer and split controls so the UI stays readable and reusable, and wire the feature dispatcher so the expense destination opens this screen while the shell navigation remains intact. Keep the form language plain and action-oriented, but ensure it feeds the domain-level draft helper without extra translation layers.</action>
  <verify>
    <automated>npx tsc --noEmit && npm test</automated>
  </verify>
  <done>The user can draft an expense with payer rows and split controls, then submit it through the shared ledger session.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| form draft -> expense event builder | User-entered expense data must be normalized before it enters replay-safe persistence |
| split editor -> payer validation | Split participant IDs and totals must match the current ledger roster before submission |
| expense route tap -> shell navigation | The expense flow should remain inside known app destinations |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-01 | T | `src/data/ledger/expenseDrafts.ts` | mitigate | Validate every required expense field and reuse the split contract before any repository append occurs. |
| T-11-02 | I | `src/screens/ExpenseEntryScreen.tsx` | mitigate | Keep the form aligned with the replay model and show domain-style validation errors rather than inventing a parallel vocabulary. |
| T-11-03 | D | `src/state/ledgerSession.tsx` | mitigate | Keep draft submission bounded to explicit user action and refresh the replayed snapshot after each save. |
</threat_model>

<verification>
- `npx tsc --noEmit`
- `npm test`
</verification>

<success_criteria>
- FE-05 is satisfied: the user can draft an expense with payer rows and equal, exact, or percentage split controls.
- Expense submission goes through the shared ledger session and replay-safe event model.
- The expense entry flow remains inside the branded shell and preserves access to the other main areas.
</success_criteria>

<output>
After completion, create `.planning/phases/11-expense-entry-and-review-ui/11-01-SUMMARY.md`
</output>