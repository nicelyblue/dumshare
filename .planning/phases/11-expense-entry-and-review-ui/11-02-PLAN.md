---
phase: 11-expense-entry-and-review-ui
plan: 02
type: execute
wave: 2
depends_on:
  - 11-01
files_modified:
  - src/data/ledger/expenseReview.ts
  - src/state/ledgerSession.tsx
  - src/screens/ExpenseEntryScreen.tsx
  - src/screens/PendingReviewScreen.tsx
  - src/screens/SubmissionDetailScreen.tsx
  - src/ui/ReviewStatusPill.tsx
autonomous: true
requirements:
  - FE-06
must_haves:
  truths:
    - "User can review pending contributor submissions and see approval state clearly in the UI."
    - "Review actions approve or reject a pending submission through the shared session rather than mutating approved entries directly in the screen."
    - "The review list and detail views stay within the expense workflow and continue to use replay-backed state."
  artifacts:
    - path: "src/data/ledger/expenseReview.ts"
      provides: "Pending-submission snapshot loader and approval/rejection action payloads"
      exports: ["loadExpenseReviewSnapshot", "buildSubmissionReviewEventInput"]
    - path: "src/state/ledgerSession.tsx"
      provides: "Shared review actions and review-state refresh hook"
      exports: ["LedgerSessionProvider", "useLedgerSession"]
    - path: "src/screens/PendingReviewScreen.tsx"
      provides: "Review list screen showing contributor submissions and status"
      exports: ["PendingReviewScreen"]
    - path: "src/screens/SubmissionDetailScreen.tsx"
      provides: "Submission detail view with approve/reject actions and summary context"
      exports: ["SubmissionDetailScreen"]
    - path: "src/ui/ReviewStatusPill.tsx"
      provides: "Reusable status chip for pending/approved/rejected states"
      exports: ["ReviewStatusPill"]
  key_links:
    - from: "src/data/ledger/expenseReview.ts"
      to: "src/domain/projections/replay.ts"
      via: "pendingSubmissions and reviewedSubmissions from replayed projection"
      pattern: "pendingSubmissions|reviewedSubmissions"
    - from: "src/state/ledgerSession.tsx"
      to: "src/data/ledger/expenseReview.ts"
      via: "approve/reject review actions and review snapshot refresh"
      pattern: "submitExpenseReview"
    - from: "src/screens/PendingReviewScreen.tsx"
      to: "src/screens/SubmissionDetailScreen.tsx"
      via: "selected submission drill-in"
      pattern: "SubmissionDetailScreen"
---

<objective>
Deliver the review slice of Phase 11: a pending-submission list, submission detail view, and approval/rejection actions that keep organizer review inside the shared replay-backed session.

Purpose: Satisfy FE-06 by letting the user inspect pending contributor submissions, understand their current approval state, and take organizer review actions without mutating approved entries directly in the UI.
Output: Pending-review snapshot helpers, shared review actions, review list/detail screens, and status-chip UI.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-expense-entry-and-review-ui/11-01-PLAN.md
@src/domain/events/types.ts
@src/domain/projections/types.ts
@src/domain/projections/replay.ts
@src/state/ledgerSession.tsx
@src/screens/ExpenseEntryScreen.tsx

<interfaces>
From `src/domain/events/types.ts`:
```typescript
export type ExpenseSubmissionReviewedPayload = {
  submissionId: string;
  decision: "approved" | "rejected";
  reviewReason: string;
};
```

From `src/domain/projections/types.ts`:
```typescript
export type LedgerProjection = {
  pendingSubmissions: ({
    submissionType: "expense-create" | "expense-amendment";
    submissionId: string;
    proposedExpense: ExpenseCreatedPayload;
  })[];
  reviewedSubmissions: {
    submissionId: string;
    decision: "approved" | "rejected";
  }[];
};
```

From `src/state/ledgerSession.tsx`:
```typescript
export function useLedgerSession(): LedgerSessionValue;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add pending-review snapshot loader and review actions to the session</name>
  <files>src/data/ledger/expenseReview.ts, src/state/ledgerSession.tsx</files>
  <action>Create a review-oriented data helper that reads the replayed projection and derives a list of pending submissions with their current status, target expense context, and status copy for the UI. Extend the shared ledger session with explicit approval/rejection actions that append `expense.submission-reviewed` events, then refresh the review snapshot so the screens stay in sync with the organizer decision. Keep the review copy aligned with the domain rules so the UI shows the same approved/pending/rejected language the ledger uses.</action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>The shared session can load review state and write organizer decisions through replay-safe approval events.</done>
</task>

<task type="auto">
  <name>Task 2: Build the pending review list and submission detail screens</name>
  <files>src/screens/PendingReviewScreen.tsx, src/screens/SubmissionDetailScreen.tsx, src/ui/ReviewStatusPill.tsx, src/screens/ExpenseEntryScreen.tsx</files>
  <action>Build a review list view that shows each pending contributor submission with a clear approval-state pill, then drill into a submission detail view that exposes the expense summary, split detail, and approve/reject controls. Reuse `ReviewStatusPill` for consistent state labeling and keep the review experience inside the expense workflow rather than inventing a separate navigation silo. Update the expense entry screen so it can surface the pending-review entry point and link into the new review views while preserving the same branded shell and ledger session behavior established earlier in the milestone.</action>
  <verify>
    <automated>npx tsc --noEmit && npm test</automated>
  </verify>
  <done>The user can review pending contributor submissions and see approval state clearly in the UI.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| replay projection -> review list | Pending submissions must be derived from canonical replay state before they are shown |
| organizer action -> approval event | Review decisions must be explicit and should not mutate approved entries directly in the UI |
| review entrypoint -> expense workflow | The review flow should remain within the expense workflow and not bypass the shell |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-04 | T | `src/data/ledger/expenseReview.ts` | mitigate | Build review state exclusively from replayed `pendingSubmissions` and `reviewedSubmissions` data. |
| T-11-05 | I | `src/screens/PendingReviewScreen.tsx` | mitigate | Use explicit state pills and detail context so the organizer can distinguish pending, approved, and rejected submissions. |
| T-11-06 | D | `src/state/ledgerSession.tsx` | mitigate | Keep approval/rejection actions bounded to explicit user taps and refresh the review snapshot after each decision. |
</threat_model>

<verification>
- `npx tsc --noEmit`
- `npm test`
</verification>

<success_criteria>
- FE-06 is satisfied: pending contributor submissions are visible with clear approval state and organizer actions.
- Review decisions flow through the shared replay-backed session and append approval events.
- The review experience stays inside the expense workflow and preserves the shell/navigation structure.
</success_criteria>

<output>
After completion, create `.planning/phases/11-expense-entry-and-review-ui/11-02-SUMMARY.md`
</output>