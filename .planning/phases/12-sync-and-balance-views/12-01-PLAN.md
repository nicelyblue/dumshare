---
phase: 12-sync-and-balance-views
plan: 01
type: execute
wave: 1
depends_on:
  - 11-02
files_modified:
  - src/data/ledger/syncSession.ts
  - src/state/ledgerSession.tsx
  - src/screens/SyncScreen.tsx
  - src/screens/FeatureScreen.tsx
  - src/navigation/featureRegistry.ts
autonomous: true
requirements:
  - FE-07
must_haves:
  truths:
    - "User can start sync by showing a QR request payload from the current ledger context."
    - "User can paste/scan a QR request payload and see plain-language validation or progress states."
    - "User can run the transfer action and see deterministic status timeline text for sending/receiving/completion."
  artifacts:
    - path: "src/data/ledger/syncSession.ts"
      provides: "UI-facing sync helpers that bridge session snapshot data with domain QR/session/exchange APIs"
      exports: ["buildSyncRequestQr", "runSyncTransfer", "parseSyncRequestQr"]
    - path: "src/state/ledgerSession.tsx"
      provides: "Shared sync state and actions for QR generation and transfer lifecycle"
      exports: ["LedgerSessionProvider", "useLedgerSession"]
    - path: "src/screens/SyncScreen.tsx"
      provides: "Branded sync screen with QR request generation, input/scan field, and transfer status timeline"
      exports: ["SyncScreen"]
    - path: "src/screens/FeatureScreen.tsx"
      provides: "Route dispatcher branch that renders SyncScreen for sync destination"
      exports: ["FeatureScreen"]
  key_links:
    - from: "src/data/ledger/syncSession.ts"
      to: "src/domain/sync/index.ts"
      via: "decodeSyncRequestQr / encodeSyncRequestQr / establishSyncSession / runBidirectionalSyncExchange"
      pattern: "decodeSyncRequestQr|encodeSyncRequestQr|establishSyncSession|runBidirectionalSyncExchange"
    - from: "src/state/ledgerSession.tsx"
      to: "src/data/ledger/syncSession.ts"
      via: "sync actions in provider"
      pattern: "runSyncTransfer|buildSyncRequestQr|parseSyncRequestQr"
    - from: "src/screens/FeatureScreen.tsx"
      to: "src/screens/SyncScreen.tsx"
      via: "screenKind dispatch branch"
      pattern: "feature\.screenKind === 'sync'"
---

<objective>
Deliver the sync-view slice of Phase 12 by adding a mobile sync screen that starts from QR request generation/scanning and runs the existing domain sync exchange with plain-language status updates.

Purpose: Satisfy FE-07 using the already-validated sync domain contracts so the frontend stays thin and replay-safe.
Output: Sync data bridge helpers, shared session sync actions, branded sync UI screen, and route wiring.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-in-person-sync-exchange/07-in-person-sync-exchange-01-SUMMARY.md
@.planning/phases/11-expense-entry-and-review-ui/11-02-SUMMARY.md
@src/domain/sync/index.ts
@src/domain/sync/types.ts
@src/domain/sync/exchange.ts
@src/state/ledgerSession.tsx
@src/screens/FeatureScreen.tsx

<interfaces>
From `src/domain/sync/types.ts`:
```typescript
export type SyncRequestQrPayload = {
  ledgerId: string;
  requesterDeviceId: string;
  lastSeenSequence: number;
  requestedAt: string;
  nonce: string;
};

export type SyncSessionDescriptor = {
  sessionId: string;
  ledgerId: string;
  organizerDeviceId: string;
  contributorDeviceId: string;
  contributorCheckpoint: number;
};
```

From `src/domain/sync/exchange.ts`:
```typescript
export type SyncExchangeResult = {
  upload: { events: StoredEvent[]; eventIds: string[] };
  download: { events: StoredEvent[]; eventIds: string[] };
  appliedRemoteEventIds: string[];
  statusTimeline: string[];
};
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add sync session bridge helpers and shared provider actions</name>
  <files>src/data/ledger/syncSession.ts, src/state/ledgerSession.tsx</files>
  <behavior>
    - Test 1: invalid QR payload returns plain-language parse error and does not run exchange.
    - Test 2: valid QR payload establishes session and returns status timeline including sending/receiving/complete.
    - Test 3: provider action refreshes replay-backed snapshot after successful sync run.
  </behavior>
  <action>Create a UI data-bridge module that reads the latest ledger/events from repository, builds QR request payload text, validates decoded request payloads, establishes organizer-authorized sync sessions, and runs bidirectional exchange using existing domain sync exports. Extend `LedgerSessionValue` with sync state/actions (`buildSyncRequestQr`, `parseSyncRequestQr`, `runSyncTransfer`) so sync UX follows the existing append-then-refresh session pattern from prior phases.</action>
  <verify>
    <automated>npx tsc --noEmit && npm test</automated>
  </verify>
  <done>Session exposes replay-safe sync actions with deterministic plain-language validation and transfer status outputs.</done>
</task>

<task type="auto">
  <name>Task 2: Build branded sync screen and wire feature dispatch</name>
  <files>src/screens/SyncScreen.tsx, src/screens/FeatureScreen.tsx, src/navigation/featureRegistry.ts</files>
  <action>Create `SyncScreen` inside `AppShell` with sections for: QR request generation/display text, QR request input/scan text area, and transfer timeline output. Keep copy plain-language and action-led (start sync, validate request, run transfer). Update `featureRegistry` and `FeatureScreen` so the sync destination renders `SyncScreen` through an explicit `screenKind: 'sync'` branch while preserving existing dashboard/setup/expense/review behavior.</action>
  <verify>
    <automated>npx tsc --noEmit && npm test</automated>
  </verify>
  <done>User can open Sync destination, prepare request payload, run transfer from a valid request, and read deterministic progress states.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| sync QR input -> sync parser | Untrusted payload text crosses into domain session bootstrap |
| session bootstrap -> exchange run | Organizer authority and ledger identity must be enforced before mutation |
| exchange results -> UI timeline | Status text must not hide failed validation or partial outcomes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-01 | T | `src/data/ledger/syncSession.ts` | mitigate | Validate/decode QR payload with domain codec before building session and reject malformed payloads with explicit errors. |
| T-12-02 | E | `src/data/ledger/syncSession.ts` | mitigate | Enforce organizer-authorized session establishment path (`establishSyncSession`) before running exchange. |
| T-12-03 | I | `src/screens/SyncScreen.tsx` | mitigate | Surface exact deterministic timeline and validation messages from domain sync results without rewriting semantic meaning. |
</threat_model>

<verification>
- `npx tsc --noEmit`
- `npm test`
</verification>

<success_criteria>
- FE-07 is satisfied: user can start sync through QR request flow and follow plain-language transfer states.
- Sync UI uses existing domain sync contracts (no duplicate sync logic in presentation components).
- Session actions refresh replay-backed snapshot after sync actions complete.
</success_criteria>

<output>
After completion, create `.planning/phases/12-sync-and-balance-views/12-01-SUMMARY.md`
</output>
