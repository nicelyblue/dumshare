---
phase: 12-sync-and-balance-views
plan: 02
type: execute
wave: 2
depends_on:
  - 12-01
files_modified:
  - src/data/ledger/balanceDetails.ts
  - src/state/ledgerSession.tsx
  - src/ui/CurrencyBalanceCard.tsx
  - src/screens/BalancesScreen.tsx
  - src/screens/FeatureScreen.tsx
  - src/navigation/featureRegistry.ts
autonomous: true
requirements:
  - FE-08
must_haves:
  truths:
    - "User can open the Balances destination and see participant balances grouped by currency without cross-currency merging."
    - "User can inspect settlement-ready paid/owed/net values with approved-only scope note when pending submissions exist."
    - "Balance detail rendering remains replay-backed and deterministic across refreshes."
  artifacts:
    - path: "src/data/ledger/balanceDetails.ts"
      provides: "UI-facing balance snapshot loader that maps domain per-currency summary into display-friendly rows"
      exports: ["loadBalanceDetailSnapshot"]
    - path: "src/ui/CurrencyBalanceCard.tsx"
      provides: "Reusable per-participant currency totals card"
      exports: ["CurrencyBalanceCard"]
    - path: "src/screens/BalancesScreen.tsx"
      provides: "Branded balances screen with participant-first per-currency settlement rows"
      exports: ["BalancesScreen"]
    - path: "src/state/ledgerSession.tsx"
      provides: "Shared state includes balance detail snapshot refresh path"
      exports: ["LedgerSessionProvider", "useLedgerSession"]
  key_links:
    - from: "src/data/ledger/balanceDetails.ts"
      to: "src/domain/balances/index.ts"
      via: "buildApprovedBalanceSummary"
      pattern: "buildApprovedBalanceSummary"
    - from: "src/screens/BalancesScreen.tsx"
      to: "src/ui/CurrencyBalanceCard.tsx"
      via: "participant card composition"
      pattern: "CurrencyBalanceCard"
    - from: "src/screens/FeatureScreen.tsx"
      to: "src/screens/BalancesScreen.tsx"
      via: "screenKind dispatch branch"
      pattern: "feature\.screenKind === 'balances'"
---

<objective>
Deliver the balance-view slice of Phase 12 by adding a balances screen that renders replay-backed per-currency settlement summaries with plain-language approved-scope context.

Purpose: Satisfy FE-08 while preserving the v1 rule that currencies remain separate and organizer-approved entries are the numeric source of truth.
Output: Balance detail snapshot loader, reusable currency balance card, balances screen, and shell route wiring.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-per-currency-balances-and-settlement-readiness/08-per-currency-balances-and-settlement-readiness-01-SUMMARY.md
@.planning/phases/12-sync-and-balance-views/12-01-SUMMARY.md
@src/domain/balances/index.ts
@src/domain/balances/types.ts
@src/domain/balances/summary.ts
@src/data/ledger/ledgerSnapshot.ts
@src/screens/FeatureScreen.tsx

<interfaces>
From `src/domain/balances/types.ts`:
```typescript
export type CurrencyBalanceRow = {
  currency: string;
  paidTotalMinor: number;
  owedTotalMinor: number;
  netMinor: number;
};

export type ParticipantCurrencyBalances = {
  participantId: string;
  displayName: string;
  balancesByCurrency: CurrencyBalanceRow[];
};
```

From `src/domain/balances/summary.ts`:
```typescript
export type ApprovedBalanceSummary = {
  participants: ParticipantCurrencyBalances[];
  metadata: {
    pendingSubmissionCount: number;
    reviewedSubmissionCount: number;
    approvalScopeNote: string;
  };
};
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add balance detail snapshot loader and session state wiring</name>
  <files>src/data/ledger/balanceDetails.ts, src/state/ledgerSession.tsx</files>
  <behavior>
    - Test 1: snapshot returns participant rows grouped per currency with no merged conversion fields.
    - Test 2: snapshot returns approved-only scope note when pending submission count is greater than zero.
    - Test 3: session refresh path keeps balance snapshot consistent with dashboard replay output.
  </behavior>
  <action>Create `loadBalanceDetailSnapshot` that reuses replay + `buildApprovedBalanceSummary` and maps participant/currency rows into plain-language UI labels while preserving raw minor-unit values. Extend session state to expose this snapshot alongside existing dashboard/review data, keeping one shared refresh lifecycle instead of separate fetch logic per screen.</action>
  <verify>
    <automated>npx tsc --noEmit && npm test</automated>
  </verify>
  <done>Session exposes deterministic balance detail data derived only from approved entries and per-currency rows.</done>
</task>

<task type="auto">
  <name>Task 2: Build balances screen and reusable per-currency cards</name>
  <files>src/ui/CurrencyBalanceCard.tsx, src/screens/BalancesScreen.tsx, src/screens/FeatureScreen.tsx, src/navigation/featureRegistry.ts</files>
  <action>Implement `BalancesScreen` in `AppShell` that renders participant-first cards and each participant's `balancesByCurrency` rows with paid/owed/net totals plus settlement-ready labels. Create `CurrencyBalanceCard` to keep row formatting consistent and explicit for positive/negative net values. Update registry and dispatcher to render `BalancesScreen` for `screenKind: 'balances'`, preserving existing sync/dashboard/setup/expense routes unchanged.</action>
  <verify>
    <automated>npx tsc --noEmit && npm test</automated>
  </verify>
  <done>User can inspect per-currency balance detail and settlement-ready summaries from the Balances destination with approved-scope context.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| replay projection -> balance snapshot mapper | Numeric settlement values cross into UI formatting layer |
| balance metadata -> approval scope message | Pending/review counts influence scope note shown to user |
| balances route dispatch -> destination rendering | Route selection controls which financial summary screen appears |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-04 | T | `src/data/ledger/balanceDetails.ts` | mitigate | Source numbers only from `buildApprovedBalanceSummary` and keep minor-unit values intact through mapper functions. |
| T-12-05 | I | `src/screens/BalancesScreen.tsx` | mitigate | Render `approvalScopeNote` exactly from summary metadata so pending-state scope cannot be hidden by UI shortcuts. |
| T-12-06 | D | `src/screens/FeatureScreen.tsx` | mitigate | Use explicit `screenKind` branch for balances route to avoid accidental fallback to generic placeholder content. |
</threat_model>

<verification>
- `npx tsc --noEmit`
- `npm test`
</verification>

<success_criteria>
- FE-08 is satisfied: user can inspect per-currency balance detail and settlement-ready summaries.
- No cross-currency merge logic is introduced; currency rows remain separate by design.
- Balance rendering remains replay-backed and approved-entry scoped.
</success_criteria>

<output>
After completion, create `.planning/phases/12-sync-and-balance-views/12-02-SUMMARY.md`
</output>
