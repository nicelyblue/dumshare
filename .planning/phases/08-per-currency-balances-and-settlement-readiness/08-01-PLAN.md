---
phase: 08-per-currency-balances-and-settlement-readiness
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/domain/balances/types.ts
  - src/domain/balances/derive.ts
  - src/tests/per-currency-balances.spec.ts
autonomous: true
requirements:
  - BALN-01
must_haves:
  truths:
    - User-visible balance math is derived from approved entries only.
    - Every participant net amount is computed per currency as paid minus owed.
    - Rejected contributor changes do not alter computed net output.
  artifacts:
    - path: src/domain/balances/types.ts
      provides: Per-currency participant balance contract types.
    - path: src/domain/balances/derive.ts
      provides: Deterministic approved-entry aggregation logic.
    - path: src/tests/per-currency-balances.spec.ts
      provides: BALN-01 executable invariants for net math.
  key_links:
    - from: src/domain/balances/derive.ts
      to: src/domain/projections/types.ts
      via: LedgerProjection input typed contract
      pattern: "LedgerProjection"
    - from: src/tests/per-currency-balances.spec.ts
      to: src/domain/balances/derive.ts
      via: direct function invocation assertions
      pattern: "derivePerCurrencyBalances"
---

<objective>
Create deterministic per-currency balance derivation contracts and implementation from approved projection entries.

Purpose: Deliver BALN-01 core math in a pure domain module that downstream settlement logic can trust.
Output: New balances domain types, derivation function, and RED→GREEN tests proving approved-entry net computation.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-per-currency-balances-and-settlement-readiness/08-CONTEXT.md
@.planning/phases/08-per-currency-balances-and-settlement-readiness/08-RESEARCH.md

<interfaces>
From `src/domain/projections/types.ts`:

```typescript
export type LedgerProjection = {
  entries: LedgerEntry[];
  participants: LedgerParticipant[];
  pendingSubmissions: ...;
  reviewedSubmissions: ...;
};

export type LedgerEntry = {
  currency: string;
  payers: { participantId: string; paidAmountMinor: number }[];
  owedShares: { participantId: string; owedAmountMinor: number }[];
};
```

From `src/domain/projections/index.ts`:

```typescript
export { replayLedger } from "./replay";
export type { LedgerEntry, LedgerProjection } from "./types";
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define per-currency balance contracts and RED tests</name>
  <files>src/domain/balances/types.ts, src/tests/per-currency-balances.spec.ts</files>
  <read_first>src/domain/projections/types.ts, src/tests/expense-split-modes.spec.ts, src/tests/organizer-approval-gate.spec.ts</read_first>
  <behavior>
    - Test 1: For one currency, participant net equals sum(paidAmountMinor) - sum(owedAmountMinor) across approved entries.
    - Test 2: For mixed currencies, EUR and USD outputs are separated and not merged (per D-06).
    - Test 3: Rejected review path leaves approved-entry-derived balances unchanged (per D-03).
  </behavior>
  <action>Create `src/domain/balances/types.ts` with explicit contracts: `CurrencyBalanceRow` (`currency`, `paidTotalMinor`, `owedTotalMinor`, `netMinor`) and `ParticipantCurrencyBalances` (`participantId`, `displayName`, `balancesByCurrency`). In `src/tests/per-currency-balances.spec.ts`, add contract assertions for exported type names and RED replay-driven invariants that call `derivePerCurrencyBalances` from `src/domain/balances/derive.ts` (which does not exist yet). Use approved replay entries only as input per D-01 and include a rejected submission sequence asserting no balance mutation per D-03.</action>
  <acceptance_criteria>
    - `src/domain/balances/types.ts` contains `export type CurrencyBalanceRow`.
    - `src/domain/balances/types.ts` contains `export type ParticipantCurrencyBalances`.
    - `src/tests/per-currency-balances.spec.ts` contains `describe("per-currency balance replay invariants"`.
    - `npm run test -- per-currency-balances` exits non-zero because derive implementation is missing (RED).
  </acceptance_criteria>
  <verify>
    <automated>npm run test -- per-currency-balances</automated>
  </verify>
  <done>RED baseline exists with failing deterministic tests that encode BALN-01 and D-01/D-03/D-06 constraints.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement deterministic approved-entry per-currency derivation (GREEN)</name>
  <files>src/domain/balances/derive.ts, src/tests/per-currency-balances.spec.ts</files>
  <read_first>src/domain/balances/types.ts, src/domain/projections/types.ts, src/tests/per-currency-balances.spec.ts</read_first>
  <behavior>
    - Test 1: Derived rows include per-currency paid/owed/net totals per participant from approved entries.
    - Test 2: Pending submissions are excluded from all net values (per D-01, D-05).
    - Test 3: Currency-specific output remains separate and deterministic across replay order (per D-06).
  </behavior>
  <action>Implement `derivePerCurrencyBalances(projection: LedgerProjection): ParticipantCurrencyBalances[]` in `src/domain/balances/derive.ts`. Iterate `projection.entries` only (per D-01). For each entry payer add to paid total; for each owed share add to owed total; compute `netMinor = paidTotalMinor - owedTotalMinor`. Initialize participant rows from `projection.participants` so known participants have stable output rows. Use `Map<participantId, Map<currency, totals>>` and return deterministic sorting by participant insertion order then currency code. Do not read `pendingSubmissions` or `reviewedSubmissions` in numeric aggregation, preserving D-03 and D-05 behavior.</action>
  <acceptance_criteria>
    - `src/domain/balances/derive.ts` contains `export function derivePerCurrencyBalances`.
    - `src/domain/balances/derive.ts` contains `for (const entry of projection.entries)`.
    - `src/domain/balances/derive.ts` does not contain `pendingSubmissions` in aggregation logic.
    - `npm run test -- per-currency-balances` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>npm run test -- per-currency-balances</automated>
  </verify>
  <done>Per-currency net derivation is deterministic, approved-only, and test-green for BALN-01 math invariants.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| replay projection → balance module | Trusted replay output enters settlement-facing aggregation logic |
| approved entry currency rows → participant net output | Currency partitioning can be tampered by logic bugs |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | T | `src/domain/balances/derive.ts` | mitigate | Aggregate strictly by `entry.currency` key and assert mixed-currency tests to block hidden merges (D-06). |
| T-08-02 | R | `src/tests/per-currency-balances.spec.ts` | mitigate | Keep deterministic replay fixtures and explicit expected nets so calculation behavior is auditable and reproducible. |
| T-08-03 | D | `src/domain/balances/derive.ts` | accept | In-memory map aggregation on local ledger scale is low-risk for DoS in v1; no unbounded external input path added in this phase. |
</threat_model>

<verification>
- `npm run test -- per-currency-balances`
- `npm test`
</verification>

<success_criteria>
- BALN-01 verified by automated tests using approved entries.
- Per-currency math is deterministic and does not merge currencies.
- Rejected submission path is covered and non-mutating for computed balances.
</success_criteria>

<output>
After completion, create `.planning/phases/08-per-currency-balances-and-settlement-readiness/08-per-currency-balances-and-settlement-readiness-01-SUMMARY.md`
</output>
