---
phase: 08-per-currency-balances-and-settlement-readiness
plan: 02
type: execute
wave: 2
depends_on:
  - 08-01
files_modified:
  - src/domain/balances/summary.ts
  - src/domain/balances/index.ts
  - src/tests/per-currency-balance-summary.spec.ts
autonomous: true
requirements:
  - BALN-02
  - BALN-03
must_haves:
  truths:
    - User-visible balance outputs are separated by currency and never auto-merged.
    - Balance output includes enough per-currency paid/owed/net detail for later settlement calculations.
    - When pending submissions exist, output includes plain-language approved-only qualifier.
  artifacts:
    - path: src/domain/balances/summary.ts
      provides: Approved-state balance summary with pending qualifier metadata.
    - path: src/domain/balances/index.ts
      provides: Stable export surface for Phase 8 balance APIs.
    - path: src/tests/per-currency-balance-summary.spec.ts
      provides: BALN-02/BALN-03 and D-02/D-04/D-05 invariants.
  key_links:
    - from: src/domain/balances/summary.ts
      to: src/domain/balances/derive.ts
      via: derivePerCurrencyBalances invocation
      pattern: "derivePerCurrencyBalances"
    - from: src/domain/balances/summary.ts
      to: src/domain/projections/types.ts
      via: pendingSubmissions length drives approved-only note
      pattern: "pendingSubmissions"
---

<objective>
Expose settlement-ready, per-currency balance summaries with explicit approved-only messaging while preserving organizer approval semantics.

Purpose: Deliver BALN-02 and BALN-03 in a reusable summary API that keeps trust boundaries visible to users and downstream settlement phases.
Output: Summary builder module, exported balance API surface, and tests for approved-only qualifier + per-currency detail completeness.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-per-currency-balances-and-settlement-readiness/08-CONTEXT.md
@.planning/phases/08-per-currency-balances-and-settlement-readiness/08-RESEARCH.md
@.planning/phases/08-per-currency-balances-and-settlement-readiness/08-01-PLAN.md

<interfaces>
From `src/domain/balances/derive.ts` (from Plan 01):

```typescript
export function derivePerCurrencyBalances(projection: LedgerProjection): ParticipantCurrencyBalances[];
```

From `src/domain/projections/types.ts`:

```typescript
export type LedgerProjection = {
  pendingSubmissions: unknown[];
  reviewedSubmissions: {
    submissionId: string;
    decision: "approved" | "rejected";
  }[];
};
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add settlement-readiness summary builder with approved-only qualifier</name>
  <files>src/domain/balances/summary.ts, src/tests/per-currency-balance-summary.spec.ts</files>
  <read_first>src/domain/balances/derive.ts, src/domain/projections/types.ts, src/tests/organizer-approval-gate.spec.ts</read_first>
  <behavior>
    - Test 1: Summary returns per-currency participant rows and preserves currency separation (D-06, BALN-02).
    - Test 2: If pending submissions exist, summary includes plain-language approved-only note (D-04).
    - Test 3: Pending amendment/reject scenarios keep currently approved values until approve event exists (D-02, D-03, D-05).
  </behavior>
  <action>Create `buildApprovedBalanceSummary(projection: LedgerProjection)` in `src/domain/balances/summary.ts` returning `{ participants, metadata }`. `participants` must come from `derivePerCurrencyBalances(projection)` and include per-currency `paidTotalMinor`, `owedTotalMinor`, and `netMinor` values (BALN-03). `metadata` must include `pendingSubmissionCount`, `reviewedSubmissionCount`, and `approvalScopeNote`. Set `approvalScopeNote` to exact string `"Balances reflect approved entries only; pending changes are not included."` when `pendingSubmissionCount > 0` (per D-04), otherwise empty string. Ensure numeric output is unaffected by `pendingSubmissions` and `reviewedSubmissions` content (D-02/D-03/D-05).</action>
  <acceptance_criteria>
    - `src/domain/balances/summary.ts` contains `export function buildApprovedBalanceSummary`.
    - `src/domain/balances/summary.ts` contains `Balances reflect approved entries only; pending changes are not included.`.
    - `src/tests/per-currency-balance-summary.spec.ts` contains `pendingSubmissionCount` assertion.
    - `npm run test -- per-currency-balance-summary` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>npm run test -- per-currency-balance-summary</automated>
  </verify>
  <done>Summary API emits settlement-ready per-currency detail with approved-only note and keeps balance math pending-safe.</done>
</task>

<task type="auto">
  <name>Task 2: Export stable balances API surface and run full regression</name>
  <files>src/domain/balances/index.ts, src/tests/per-currency-balance-summary.spec.ts</files>
  <read_first>src/domain/sync/index.ts, src/domain/balances/derive.ts, src/domain/balances/summary.ts</read_first>
  <action>Create `src/domain/balances/index.ts` with explicit exports for `derivePerCurrencyBalances`, `buildApprovedBalanceSummary`, and public balance types from `src/domain/balances/types.ts`. Update summary test imports to consume from `src/domain/balances/index.ts` so downstream modules use the stable barrel. Keep existing behavior unchanged while proving integration via full suite run.</action>
  <acceptance_criteria>
    - `src/domain/balances/index.ts` contains `export { derivePerCurrencyBalances }`.
    - `src/domain/balances/index.ts` contains `export { buildApprovedBalanceSummary }`.
    - `src/tests/per-currency-balance-summary.spec.ts` imports from `../domain/balances` (barrel).
    - `npm test` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>npm test</automated>
  </verify>
  <done>Balances domain exports are stable for downstream settlement phases and BALN tests pass in full regression.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| approved balances → user-facing settlement decisions | Output messaging and data shape influence user financial actions |
| projection review queues → summary metadata | Unapproved queue state is surfaced as guidance but must not alter totals |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-04 | I | `src/domain/balances/summary.ts` | mitigate | Expose explicit `approvalScopeNote` when pending submissions exist so users are not misled by hidden state exclusions (D-04). |
| T-08-05 | T | `src/domain/balances/summary.ts` | mitigate | Keep `participants` sourced from `derivePerCurrencyBalances` only; metadata path cannot mutate numeric totals (D-02/D-05). |
| T-08-06 | R | `src/tests/per-currency-balance-summary.spec.ts` | mitigate | Add deterministic assertions for pending/rejected scenarios and exact note string so behavior is traceable and verifiable. |
</threat_model>

<verification>
- `npm run test -- per-currency-balance-summary`
- `npm run test -- per-currency-balances`
- `npm test`
</verification>

<success_criteria>
- BALN-02 verified: currencies remain separate in output and no merge path exists.
- BALN-03 verified: per-currency paid/owed/net detail is present for every participant row.
- D-04 approved-only note appears whenever pending submissions exist, with deterministic exact text.
</success_criteria>

<output>
After completion, create `.planning/phases/08-per-currency-balances-and-settlement-readiness/08-per-currency-balances-and-settlement-readiness-02-SUMMARY.md`
</output>
