---
phase: 06-organizer-approval-gate
plan: 02
type: execute
wave: 2
depends_on:
  - 06-01
files_modified:
  - src/tests/organizer-approval-gate.spec.ts
  - src/tests/expense-capture-foundations.spec.ts
  - src/tests/contributor-amendments.spec.ts
autonomous: true
requirements:
  - APRV-05
must_haves:
  truths:
    - "Rejected contributor submissions do not alter approved balances/settlement outcomes (APRV-05)."
    - "Approved contributor submissions become part of approved ledger state used downstream (APRV-05)."
  artifacts:
    - path: "src/tests/organizer-approval-gate.spec.ts"
      provides: "APRV-05 invariants for approved vs rejected state effects"
    - path: "src/tests/expense-capture-foundations.spec.ts"
      provides: "Updated expectation that contributor-created changes are pending until review"
    - path: "src/tests/contributor-amendments.spec.ts"
      provides: "Approval-path amendment application and rejection-path non-mutation assertions"
  key_links:
    - from: "src/tests/expense-capture-foundations.spec.ts"
      to: "src/domain/projections/replay.ts"
      via: "contributor-created proposal enters pending rather than entries"
      pattern: "pendingSubmissions|creatorRole"
    - from: "src/tests/contributor-amendments.spec.ts"
      to: "src/domain/projections/replay.ts"
      via: "review decision events drive apply vs no-op behavior"
      pattern: "submission-reviewed|decision"
    - from: "src/tests/organizer-approval-gate.spec.ts"
      to: "approved ledger outcomes"
      via: "entry count and content assertions before/after rejection"
      pattern: "entries\.|toHaveLength"
---

<objective>
Harden approval-gate verification so approved-state balance inputs change only via organizer approval decisions.

Purpose: Lock APRV-05 with explicit automated invariants proving reject paths are non-mutating and approve paths are mutating for downstream settlement/balance phases.
Output: Expanded phase tests plus migrated pre-Phase-6 expectations that assumed direct contributor entry application.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-organizer-approval-gate/06-RESEARCH.md
@.planning/phases/06-organizer-approval-gate/06-01-PLAN.md
@src/tests/organizer-approval-gate.spec.ts
@src/tests/expense-capture-foundations.spec.ts
@src/tests/contributor-amendments.spec.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add APRV-05 non-mutation/mutation invariants for review outcomes</name>
  <files>src/tests/organizer-approval-gate.spec.ts, src/tests/contributor-amendments.spec.ts</files>
  <read_first>src/tests/organizer-approval-gate.spec.ts, src/tests/contributor-amendments.spec.ts, src/domain/projections/replay.ts, .planning/phases/06-organizer-approval-gate/06-RESEARCH.md</read_first>
  <behavior>
    - Test 1: Rejected `expense-create` submission keeps approved `entries` unchanged.
    - Test 2: Approved `expense-create` submission adds exactly one approved entry with expected fields.
    - Test 3: Rejected `expense-amendment` keeps target entry unchanged.
    - Test 4: Approved `expense-amendment` updates target entry content deterministically.
  </behavior>
  <action>Extend `src/tests/organizer-approval-gate.spec.ts` and `src/tests/contributor-amendments.spec.ts` with explicit decision-event scenarios using `expense.submission-reviewed` payloads. Add assertions on `projection.entries` length and stable field values before and after reject/approve transitions. Assert reviewed metadata presence and pending queue removal for both decisions to prove deterministic completion of review workflow without hidden state transitions.</action>
  <acceptance_criteria>
    - Test files contain explicit assertions comparing `entries` state before and after reject decisions.
    - Test files contain explicit approve assertions for both create and amendment submission types.
    - Assertions verify pending queue is emptied for reviewed submission IDs.
  </acceptance_criteria>
  <verify>
    <automated>npm run test -- organizer-approval-gate && npm run test -- contributor-amendments</automated>
  </verify>
  <done>APRV-05 invariants are encoded as deterministic automated tests for both submission categories.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Update legacy contributor-expense expectations and run full regression suite</name>
  <files>src/tests/expense-capture-foundations.spec.ts</files>
  <read_first>src/tests/expense-capture-foundations.spec.ts, src/tests/organizer-approval-gate.spec.ts, src/domain/projections/replay.ts</read_first>
  <behavior>
    - Test 1: Contributor-created expense without organizer review is pending, not approved entry.
    - Test 2: Existing organizer-created expense behavior remains valid.
    - Test 3: Full suite remains green after expectation migration.
  </behavior>
  <action>Update contributor-path assertions in `src/tests/expense-capture-foundations.spec.ts` so contributor-originated submissions are expected in `pendingSubmissions` until review, while organizer-originated expenses still appear in approved `entries`. Preserve EXPS-01/02/03 contract checks, but align the contributor-flow expected projection shape with Phase 6 approval gate semantics to avoid false regressions in downstream phases.</action>
  <acceptance_criteria>
    - `expense-capture-foundations.spec.ts` no longer expects contributor-created expense to auto-append into `entries` before review.
    - Organizer-created flow assertions are unchanged and still pass.
    - Full repository test run succeeds.
  </acceptance_criteria>
  <verify>
    <automated>npm test</automated>
  </verify>
  <done>Regression suite confirms only organizer-approved contributor changes influence approved ledger outcomes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| replay approval decisions -> approved ledger assertions | Incorrect tests can mask unauthorized state mutations |
| contributor create flow expectations -> downstream feature assumptions | Legacy assumptions may hide approval bypasses |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-05 | R | phase test suites | mitigate | Add explicit approve/reject assertions with deterministic before/after entry comparisons. |
| T-06-06 | T | legacy contributor tests | mitigate | Migrate contributor expense expectations to pending-first semantics and keep organizer path unchanged. |
| T-06-07 | I | balance-input invariants | mitigate | Require full-suite run (`npm test`) to catch hidden mutation paths affecting approved state consumers. |
</threat_model>

<verification>
- `npm run test -- organizer-approval-gate`
- `npm run test -- contributor-amendments`
- `npm test`
</verification>

<success_criteria>
- APRV-05: Reject decision paths are proven non-mutating for approved entries.
- APRV-05: Approve decision paths are proven mutating for approved entries and available to downstream balance computation.
- Legacy phase tests remain green under the new approval-gated contributor flow.
</success_criteria>

<output>
After completion, create `.planning/phases/06-organizer-approval-gate/06-organizer-approval-gate-02-SUMMARY.md`
</output>
